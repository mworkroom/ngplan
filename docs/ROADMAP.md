# 피라미드앱 개발 로드맵

> 기준 요구사항: `docs/requirements/pyramid-app-requirements-v2.md`  
> 작성일: 2026-07-11  
> 대상 서비스: `https://plan.nangok.app`  
> 배포 저장소: `https://mworkroom.github.io/ngplan`  
> 현재 상태: Phase 3 수동 계획 작업표 구현·자동 검증 완료, 엄마 확인이 필요한 표시 보완은 Phase 4 화면 작업과 함께 진행, Phase 4 PRE-WP0 계약 동기화 진행 중

## 1. 로드맵 목적

이 문서는 요구사항 확정부터 최종 운영 배포까지의 전체 개발 순서와 각 Phase의 완료 기준을 정의한다.

가장 큰 제품 위험은 화면 구현이 아니라 다음 계산을 동시에 정확하게 유지하는 데 있다.

- 하위 조직 실적의 상위 연쇄 합산
- 커미션 발생 여부에 따라 초기화 또는 이월되는 일일 장부
- 일일 초기화와 무관하게 원본 실적을 보존하는 보름 장부
- 최소 신규 PV를 찾는 자동 계획과 실제값 이후의 부분 재시뮬레이션

따라서 Phase 1에서 결정론적 계산 코어를 먼저 완성하고, 이후 모든 화면과 자동화 기능이 같은 코어를 사용하도록 한다.

## 2. 진행 원칙

1. 요구사항에서 확정되지 않은 규칙은 임의로 제품 규칙으로 만들지 않는다.
2. 계산 코어는 UI, 저장소, 네트워크에 의존하지 않는 순수 로직으로 유지한다.
3. 날짜별 원본 입력과 계산된 파생값을 분리하며 원본을 계산 결과로 덮어쓰지 않는다.
4. 모든 계산 결과는 어떤 입력과 규칙 버전에서 나왔는지 추적할 수 있어야 한다.
5. 자동 계획이 만든 결과도 독립된 계산 코어로 다시 검증한다.
6. 과거 확정 기록은 당시 데이터와 규칙 버전을 보존하며 현재 규칙으로 조용히 바꾸지 않는다.
7. 각 Phase는 이전 Phase의 자동 테스트와 완료 기준을 깨뜨리지 않은 상태에서 종료한다.
8. 수당 자격 PVP, 일일 PVP 잔액, 보름 PVP는 서로 다른 장부 의미로 저장하고 계산한다.
9. 자동 계획의 비즈니스 순서는 화면 표시 순서와 분리하며, 안정적인 회원 키와 조직 구조에서 파생한 순서만 사용한다.

## 3. 전체 Phase 한눈에 보기

| Phase | 이름 | 핵심 결과 | 상태 |
|---:|---|---|---|
| 준비 | 요구사항·설계 기준선 | 로드맵, 기술 설계, 계산 정답 사례, Phase 1 실행 계획 | 완료 |
| 1 | 결정론적 계산 코어 | 수동 입력을 조직·일일·보름 규칙대로 계산하는 검증된 엔진 | 완료 — #1 |
| 2 | 프로젝트와 조직 입력 | 반월 프로젝트 생성, 회원·조직·시작값 입력 및 검증 | 완료 |
| 3 | 수동 계획 작업표 | 가로형 표에서 계획을 편집하고 모든 계산 결과 확인 | 구현 완료 · 전체 수동 브라우저 검증 진행 중 |
| 4 | 자동 계획 최적화 | 목표를 만족하는 최소 신규 PV 계획 생성 | 진행 중 — PRE-WP0 계약 동기화 |
| 5 | 확정·실제·부분 재시뮬레이션 | 실행 결과 반영과 미래 계획의 최소 변경 재계산 | 대기 |
| 6 | 저장·종료 기록·내보내기 | 진행 중 복구와 읽기 전용 과거 기록 보존 | 대기 |
| 7 | 릴리스 후보 검증 | 정확성, 성능, 사용성, 접근성, 복구 절차 검증 | 대기 |
| 8 | 프로덕션 배포 | GitHub Pages 및 사용자 도메인 운영 배포 | 대기 |

Phase는 원칙적으로 순서대로 진행한다. 단, Phase 4의 최적화 방식 연구와 Phase 6의 저장 방식 검토처럼 앞선 Phase의 계약을 변경하지 않는 조사 작업은 병행할 수 있다.

---

## 4. Phase 1 — 결정론적 계산 코어

### 4.1 목표

주어진 조직, 시작값, 날짜별 직접 배정값을 받아 항상 같은 일일·보름 결과를 반환하는 계산 엔진을 만든다. 자동으로 실적을 배정하는 기능은 포함하지 않는다.

### 4.2 구현 전 진입 조건

완료된 조건:

- `TECHNICAL_DESIGN.md`의 **Q-CALC-01~Q-CALC-06** 결정이 반영되었다.
- 계산용 레벨 개념은 제거했다. 회원별 PVP 목표는 2,400·1,500·700 중 직접 선택하며, 목표 700에만 약 8일 분산 선호를 적용한다.
- 이름 앞 번호와 색상은 계산과 무관한 선택형 찾기 표지판이다.
- 프로젝트는 루트 한 명의 연결된 트리이며 일요일은 `SKIP_NO_INPUT`으로 확정되었다.
- Phase 1 기술 스택은 TypeScript, Vite, Vitest로 승인되었다. UI 프레임워크는 Phase 2 전에 결정한다.
- `CALCULATION_CASES.md`의 Phase 1 계산 규칙 차단 사례가 모두 확정되었다.
- 계산 규칙 버전은 `2.0.0`으로 확정되었다.

완료된 작업 관리 조건:

- 구현 작업을 GitHub Issue #1에 연결했다.

### 4.3 상세 범위

1. 상반기 1~15일과 하반기 16일~월말 날짜를 생성하고 일요일을 식별한다.
2. 회원, 상위 회원, 좌·우 배치, `스스로` 방향, PVP 목표, 찾기 표지판, 수당 자격 PVP·보름 PVP·일일 PVP·일일 좌·우의 다섯 종류 시작값을 표현한다.
3. 중복 회원 ID, 중복 슬롯, 고아 회원, 순환, 연결되지 않은 조직, 연결 방향 직접 입력을 검증한다.
4. 하루의 직접 입력을 하위부터 합산하여 각 회원의 신규 PVP·조직 좌·조직 우 원본값을 계산한다.
5. 회원별 전일 잔액과 당일 원본값으로 정산 전 잔액을 만든다.
6. PVP를 판정용 작은 쪽에 전량 적용하고 공식 단계 중 가장 높은 일일 커미션 단계를 판정한다.
7. 커미션 발생 시 PVP·좌·우와 초과분을 모두 0으로 만들고, 미발생 시 원래 세 잔액을 그대로 이월한다.
8. 일일 초기화와 별도로 날짜별 원본값을 누적하여 개인 PVP 목표와 보름 좌·우 목표를 판정한다.
9. 전체 반월을 날짜순으로 계산하고, 각 중간값을 감사 가능한 결과로 반환한다.
10. 확정된 계산 사례를 단위·통합 테스트로 자동 검증한다.

### 4.4 파일 단위 실행 지시

아래 경로는 확정된 TypeScript + Vite + Vitest 기준의 Phase 1 변경 계획이다.

| 경로 | 작업 지시 |
|---|---|
| `package.json` | 개발, 타입 검사, 테스트, 커버리지, 빌드 명령과 Phase 1 의존성을 정의한다. |
| `package-lock.json` | 승인된 의존성의 재현 가능한 버전을 잠근다. |
| `tsconfig.json` | 엄격한 TypeScript 검사와 브라우저 대상 모듈 설정을 정의한다. |
| `vite.config.ts` | 로컬 실행과 정적 빌드의 최소 구성을 정의한다. 배포 자동화는 넣지 않는다. |
| `vitest.config.ts` | 계산 테스트의 실행 환경, 포함 경로, 커버리지 기준을 정의한다. |
| `index.html` | Vite 진입점이 되도록 최소 변경한다. 제품 화면은 구현하지 않는다. |
| `src/main.ts` | 계산 코어를 손으로 실행하지 않아도 빌드 가능함을 확인하는 최소 진입점만 둔다. |
| `src/domain/types.ts` | 기간, 회원, 배치, 시작 잔액, 직접 실적, 원본 합산값, 일일 결과, 보름 결과 타입을 정의한다. |
| `src/domain/constants.ts` | 커미션 단계, 허용 PVP 목표, 목표 700의 약 8일 권장, 좌·우 2,500 목표, 규칙·엔진 버전을 정의한다. |
| `src/domain/pv.ts` | 0 이상 안전 정수 PV 생성과 모든 합산의 범위 검사를 제공한다. |
| `src/domain/period.ts` | 반월 경계, 월말·윤년, 날짜 순서, 일요일 여부를 계산한다. |
| `src/domain/validation.ts` | 숫자와 날짜 입력 및 조직 불변조건을 계산 전에 검증한다. |
| `src/engine/organization.ts` | 후위 순회 계산 순서와 날짜별 하위 실적의 상위 연쇄 합산을 구현한다. |
| `src/engine/daily-ledger.ts` | 정산 전 잔액, PVP 적용, 단계 판정, 초기화·이월을 계산한다. 최적화용 초과 지표는 Phase 4로 미룬다. |
| `src/engine/half-month-ledger.ts` | 날짜별 원본 누적, PVP 목표, 마감 PVP 적용, 좌·우 목표를 계산한다. |
| `src/engine/calculate-period.ts` | 날짜순으로 조직 합산·일일 정산·보름 누적을 조합하고 전체 추적 결과를 반환한다. |
| `src/engine/index.ts` | 후속 UI와 최적화기가 사용할 공개 계산 API만 노출한다. |
| `src/domain/__tests__/pv.test.ts` | 개별 PV와 파생 합계의 정수·범위 경계를 검증한다. |
| `src/domain/__tests__/period.test.ts` | 반월, 월말·윤년, 확정된 일요일 정책을 검증한다. |
| `src/domain/__tests__/validation.test.ts` | 숫자·날짜·회원·조직·편집 방향 오류를 검증한다. |
| `src/engine/__tests__/organization.test.ts` | 단일·양쪽·다단계 조직 전파와 잘못된 조직 거부를 검증한다. |
| `src/engine/__tests__/daily-ledger.test.ts` | 모든 단계와 경계값, PVP 적용, 초기화, 초과분 소멸, 이월을 검증한다. |
| `src/engine/__tests__/half-month-ledger.test.ts` | 시작값 분리, 원본 누적, 마감 PVP 적용과 목표 판정을 검증한다. |
| `src/engine/__tests__/calculate-period.test.ts` | 여러 날짜, 일요일, 월말, 조직 전파와 두 장부의 통합 동작을 검증한다. |
| `.github/workflows/ci.yml` | 타입 검사, 테스트, 커버리지 기준, 빌드를 자동 검증한다. 운영 배포 단계는 넣지 않는다. |
| `docs/devlog/YYYY-MM-DD.md` | 실제 구현 내용, 이유, 결정 과정, 테스트와 남은 제한을 한국어로 기록한다. |

### 4.5 작업 순서

1. 완료된 계산 계약과 규칙 버전 `2.0.0`을 구현 Issue에 연결한다.
2. 프로젝트 도구와 엄격한 타입 검사를 구성한다.
3. 도메인 타입, 상수, 입력 검증을 만든다.
4. 조직 전파 계산을 구현하고 독립 테스트를 통과시킨다.
5. 일일 장부를 구현하고 공식 단계 전체와 경계를 검증한다.
6. 보름 장부를 구현하고 일일 장부와 독립적으로 원본이 보존되는지 검증한다.
7. 기간 계산기를 조합하고 다일·다단계 조직 통합 사례를 검증한다.
8. 전체 타입 검사, 테스트, 커버리지 검사, 빌드를 실행하고 개발 로그를 작성한다.

### 4.6 완료 기준

- `CALCULATION_CASES.md`에서 Phase 1 대상으로 표시된 모든 확정 사례가 자동 테스트로 통과한다.
- 300, 700, 1,500, 2,400, 6,000, 20,000, 60,000의 직전·정확한 경계를 검증한다.
- 커미션 발생 시 초과분까지 모두 소멸하고 미발생 시 PVP·좌·우가 원래 항목으로 이월된다.
- 일일 잔액 초기화가 보름 원본 누적값을 지우지 않는다.
- 하위 실적은 모든 상위 회원에게 날짜당 한 번만 반영된다.
- 연결 방향은 파생값이며 PVP와 `스스로` 방향만 직접 입력할 수 있다.
- 계산 함수가 입력 데이터를 변경하지 않고 같은 입력·규칙 버전에 같은 결과를 반환한다.
- 잘못된 값과 조직은 부분 결과를 만들기 전에 식별 가능한 오류로 거부된다.
- 타입 검사, 전체 테스트, 도메인·계산 규칙 분기 커버리지 95%, 프로덕션 빌드가 통과한다.
- 실제 변경과 결정 과정이 오늘자 한국어 개발 로그에 기록된다.

### 4.7 제외 범위

- 자동 신규 PV 배정과 최적화
- 완성형 조직 편집 화면과 가로형 작업표
- 계획 확정, 실제값 입력, 차이 비교, 부분 재시뮬레이션
- 영구 저장, 계정, 기기 간 동기화, 종료 기록
- PDF·스프레드시트 등 전달용 출력
- 운영 배포

---

## 5. Phase 2 — 프로젝트와 조직 입력

### 목표

반월 프로젝트를 만들고 피라미드 그림을 보며 회원, 좌·우 연결, `스스로` 방향, PVP 목표, 찾기 표지판과 시작값을 역할별로 입력할 수 있게 한다.

### 주요 결과

- 상·하반기 `IN_PROGRESS` 프로젝트 생성, 기간 기반 기본 제목과 수동 제목 보존
- 불완전한 문자열과 임시 미배치 상태를 허용하는 인메모리 `ProjectSetupDraft`
- 회원 ID·이름·PVP 목표·찾기 표지판과 부모 기준 좌·우 배치의 카드형 편집
- 각 카드의 명시적 왼쪽·오른쪽 `+` 슬롯과 파생 `SELF/CHILD` 표시
- 수당 자격 PVP 시작값, 보름 PVP 시작값, 일일 PVP·좌·우 잔액의 다섯 칸 분리 입력, 0 기본값과 회사 시스템 확인 상태
- 중간 회원 제외 시 후손 서브트리를 삭제하지 않는 재배치 대기열과 한 자식 명시적 승격
- Phase 1의 기간·조직 검증을 재사용한 오류·경고 피드백과 불변 `ProjectSetupBundle`
- 어떤 초안 변경도 활성 설정 묶음을 즉시 해제하는 `EDITING/READY` 전이
- 이전 프로젝트 값의 자동 이월 방지와 현재 탭 `sessionStorage` 작업 스냅샷 안내. 화면 이동과 새로고침은 보호하지만 탭을 닫으면 사라진다.
- React 카드 UI, 키보드 조작, 오류 위치 이동, 반응형 배치와 넓은 트리 가로 스크롤

### 종료 기준

유효한 단일 루트·연결·비순환 조직과 확인된 시작값으로 Phase 3가 소비할 설정 묶음을 만들 수 있다. 잘못된 연결, 미배치 서브트리, 중복 회원 ID, 미확인 시작값은 게시 전에 차단되고, 경고는 보존된다. 회원 제외와 재연결은 살아 있는 후손 데이터를 다시 입력하지 않고 수행할 수 있다.

### Phase 경계

- Phase 2는 날짜별 배정 셀, 계획표와 계산 결과를 만들지 않는다.
- 계획 리비전이 생기기 전의 인메모리 설정만 자유롭게 편집한다.
- 계획 이후 조직 변경은 안정적인 회원 키와 비파괴적 서브트리 명령을 이용해 Phase 5에서 새 조직 스냅샷과 재계산으로 처리한다.
- 저장·복구·`CLOSED`·읽기 전용 기록은 Phase 6에서 처리한다.

---

## 6. Phase 3 — 수동 계획 작업표

### 목표

기존 엑셀과 유사한 가로형 표에서 날짜별 직접 실적을 편집하고 계산 결과를 즉시 확인할 수 있게 한다.

### 주요 결과

- 회원별 PVP·좌·우 열과 반월 날짜 행
- PVP와 `스스로` 방향의 1 PV 단위 입력
- 연결 방향의 읽기 전용 조직 합산값
- PVP 적용 방향, 일일 단계·발생 여부, 정산 후 잔액 표시
- 보름 누적, 목표 잔여량, 좌·우 달성 여부 표시
- 큰 표의 고정 헤더, 탐색, 입력 오류 안내

### 종료 기준

사용자가 만든 전체 반월 계획을 Phase 1 엔진으로 끝까지 계산하고 요구사항에 명시된 결과를 화면에서 대조할 수 있다.

---

## 7. Phase 4 — 자동 계획 최적화

### 목표

조직과 시작값을 기준으로 목표를 충족하는 최소 신규 PV 계획을 자동 생성한다.

### 주요 결과

- 최대 50명 조직에서 브라우저 작업 스레드로 실행하는 고정 30분 단일 자동 계획. 3시간·사용자 지정·숨은 연장 모드는 제공하지 않는다.
- 개인 PVP 및 보름 좌·우 목표와 일요일 신규 배정 금지를 하드 제약으로 다루는 계획 생성
- 수당 자격 PVP는 `수당 자격 PVP 시작값 + 해당 날짜까지의 직접 신규 PVP 누계`로 계산하고, 당일 신규 PVP를 포함한 값이 300 이상일 때만 그날 정상 커미션을 허용한다.
- PVP 300 미만의 기계적 정산은 실제 초기화 결과를 계산 엔진에 보존한다. 자동 후보에서는 금지하고, 수동 계획에서는 계산을 막는 안내로 표시한다.
- 일일·보름 PVP 모두 현재 규칙에 따라 좌·우 중 작은 쪽에 전량 적용한다.
- 상위 회원에게 함께 기여하는 배치를 활용하되 직접 신규 PV는 입력 위치에서 한 번만 비용으로 센다.
- 목적 순서는 `총 신규 PV 최소화 → 일일 소멸 초과분 최소화 → 목표 700 회원 중 8일 이상 달성 인원 최대화와 전체 일수 오름차순 벡터 최대화 → 0이 아닌 직접 입력 중 100의 배수가 아닌 칸 최소화 → 최대 직접 PVP 칸 최소화 → 정본 순서 타이브레이커`다.
- 목표 700 회원의 총 커미션 일수는 화면 표시용 합계일 뿐 목적함수 단계가 아니다.
- PVP 값이 정확히 100인 칸을 독립적으로 선호하지 않는다. 100단위 가독성은 더 높은 모든 목적이 같은 경우에만 적용한다.
- 자동 계획의 정본 회원 순서는 맨 위 회원부터 시작해 각 회원의 왼쪽 가지를 오른쪽 가지보다 먼저 방문한다. 수동 작업표의 가운데 정렬용 중위 순회 화면 순서와 혼동하지 않는다.
- 기간 마지막의 이월 잔액은 계산 엔진 결과대로 보존하며, 명시적인 소멸 규칙이 없는 한 끝 잔액 벌점이나 낭비로 계산하지 않는다.
- 최적 증명 완료와 제한 시간 안에 찾은 검증 계획을 구분하고, 후보 전체를 Phase 1 계산 코어로 독립 검증한다.
- 현재 탭에는 최신 검증 후보와 최소 호환 정보만 체크포인트한다. 전체 탐색 상태나 증명 진행을 저장·복원했다고 표시하지 않는다.

### 종료 기준

1·10·20·50명 대표 조직에서 목표 충족과 정확한 목적 순서를 재현하고, 생성·복원·적용되는 모든 후보를 버전이 맞는 Phase 1 계산 코어로 독립 검증할 수 있다. 고정 30분 안에 증명이 끝나지 않으면 검증된 최선 후보만 `최적 미증명` 상태로 제공하며, 검증 후보가 없으면 그 사실을 설명한다.

---

## 8. Phase 5 — 확정 계획, 실제 결과, 부분 재시뮬레이션

### 목표

확정 계획을 실행 결과와 비교하고, 실제값이 달라진 경우 완료 구간을 고정한 채 미래 계획만 최소 변경한다.

### 주요 결과

- 계획 중·확정 계획·실제 결과의 분리와 버전 추적
- 확정 계획을 실제값 초안으로 복사한 뒤 차이 수정
- 실제 완료 경계까지의 값과 정산 결과 고정
- 계획 이후 회원 이탈·조직 변경 시 기존 후손 데이터를 보존한 새 조직 스냅샷 생성
- 조직 변경과 기존 계획·확정·실제 리비전의 무효화·복제·재계산 관계 기록
- 미래 계획의 변경 셀 수, 변경 회원 수, 추가 신규 PV 최소화
- 원래 계획과 재시뮬레이션 결과의 차이 표시
- 회복 불가 시 부족량과 원인 표시

### 종료 기준

과거 실제값과 그 정산은 바뀌지 않고 미래의 필요한 부분만 변경되며, 변경 이유와 목표 회복 결과를 확인할 수 있다.

---

## 9. Phase 6 — 저장, 종료 기록, 복구와 내보내기

### 목표

진행 중 작업을 안전하게 복구하고 종료된 프로젝트를 당시 상태 그대로 읽기 전용 보관한다.

### 주요 결과

- 저장 방식 결정에 맞춘 자동 저장과 복구
- 데이터 스키마, 계산 규칙 버전과 당시 RuleSet 본문 보존
- 종료 당시 조직·시작값·확정 계획·실제값·계산 결과 스냅샷
- 종료 프로젝트 잠금과 새 프로젝트 자동 이월 금지
- 합의된 전달·백업 형식의 내보내기와 가져오기
- 손상·버전 불일치 데이터에 대한 안전한 오류 처리

### 종료 기준

앱 재실행 뒤 진행 작업을 복구할 수 있고, 종료 기록은 계산 규칙이 바뀌어도 당시 결과를 유지하며, 백업본으로 복원할 수 있다.

---

## 10. Phase 7 — 릴리스 후보 품질 검증

### 목표

실제 업무 규모에서 정확성, 성능, 사용성, 접근성, 데이터 안전성을 검증하고 운영 절차를 준비한다.

### 주요 결과

- 전체 계산·최적화 정답 사례 회귀 검증
- 실제 규모 조직과 반월 데이터의 성능 측정
- 대형 가로표 탐색과 핵심 흐름 사용성 검증
- 키보드 사용, 명도, 오류 안내 등 접근성 점검
- 지원 브라우저와 최소 화면 크기 확정
- 저장 실패, 데이터 손상, 배포 실패, 롤백 훈련
- 개인정보 취급과 백업·복구 절차 점검

### 종료 기준

치명적 결함이 없고, 합의된 지원 환경에서 핵심 업무 흐름과 전체 회귀 테스트가 통과하며, 복구와 롤백 절차가 검증된다.

---

## 11. Phase 8 — 프로덕션 배포

### 목표

검증된 릴리스 후보를 GitHub Pages와 사용자 도메인에 배포하고 지속 가능한 릴리스 절차를 확립한다.

### 주요 결과

- 검증을 통과한 버전만 배포하는 자동 릴리스
- GitHub Pages 주소와 `plan.nangok.app` 연결
- HTTPS, 직접 경로 접근, 새로고침, 캐시 갱신, 오류 페이지 확인
- 배포 버전과 소스 변경 이력의 추적
- 배포 후 핵심 흐름 스모크 테스트
- 이전 정상 버전으로 되돌리는 절차

### 최종 완료 기준

- 운영 주소에서 프로젝트 생성부터 조직 입력, 계획, 실제 반영, 종료 기록 열람까지 핵심 흐름이 정상 작동한다.
- 배포 버전과 계산 규칙 버전을 추적할 수 있다.
- 오류 발생 시 데이터 손실 없이 이전 정상 버전으로 복구할 수 있다.
- 운영 및 사용자 안내 문서가 최신 상태다.

## 12. Phase 간 결정 게이트

| 결정 시점 | 반드시 확정할 내용 | 영향 Phase |
|---|---|---|
| Phase 1 착수 전 — 확정 완료 | PVP 원천 범위, 조직 전파와 이월 관계, 동률, PVP 목표·찾기 표지판, 루트, 일요일 | 1 이후 전체 |
| Phase 2 착수 전 — 확정 완료 | 계획 리비전 전 `IN_PROGRESS` 자유 편집, 변경 시 설정 묶음 해제, 비파괴적 회원 제외 | 2 |
| Phase 4 착수 전 — 확정 완료 | 최대 50명, 고정 30분 단일 실행, PVP 300 자격, 다섯 시작값, 현재 작은 쪽 적용, 초과분 수식, 목표 700 임계값·벡터, 100단위·최대 PVP·정본 순서 타이브레이커, 현재 탭 체크포인트 | 4, 5, 7 |
| Phase 5 착수 전 | 실제값 완료 경계, 빈칸과 0, 재시뮬레이션 하드 제약, 계획 이후 조직 변경과 리비전 관계 | 5, 6 |
| Phase 6 착수 전 | 단일 기기 또는 계정·동기화, 개인정보, 백업·전달 형식 | 6, 7, 8 |
| Phase 8 착수 전 | 운영 도메인·DNS 권한, 지원 브라우저, 릴리스·롤백 책임 | 8 |

Phase 1 확정 결정과 이후 Phase의 미확정 질문은 `TECHNICAL_DESIGN.md`의 **구현 전 결정 목록**을 정본으로 사용한다.
