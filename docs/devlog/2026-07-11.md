# 2026-07-11 피라미드앱 Development Log

## Work Summary

통합 요구사항 v2를 구현 가능한 단계와 계산 계약으로 정리하기 위해 전체 로드맵, 기술 설계, 계산 정답 사례, Phase 1 실행 계획을 작성했다. 처음에는 불명확한 경계 조건을 질문과 `정답 보류` 사례로 분리했고, 이후 사용자 답변과 기존 SRM 엑셀 수식을 대조하여 Phase 1 계산 계약을 최종 확정했다. 실제 애플리케이션 코드는 변경하지 않았다.

## Work Completed

- 요구사항의 전체 범위를 Phase 1 계산 코어부터 Phase 8 프로덕션 배포까지 나누고, 구체적인 파일 변경 지시는 Phase 1에만 작성했다.
- 권위 있는 원본과 파생값, 조직 스냅샷, 네 종류의 시작값, 날짜별 직접 입력, 일일 정산, 보름 진행 누계와 최종 평가의 데이터 경계를 정의했다.
- 하위 조직 원본의 후위 전파, PVP의 가상 적용, 공식 커미션 단계, 전량 초기화·항목별 이월, 보름 원본 누적의 계산 순서를 설계했다.
- 자동 계획의 사전식 목적함수, 부분 재시뮬레이션의 고정 범위, 리비전·종료 스냅샷·저장 어댑터의 후속 설계를 정리했다.
- 조직, 일일 장부, 보름 목표, 달력, 레벨 4 이상 권장 횟수, 자동 계획, 실제값 재계산, 입력 검증에 대한 입력·기대 결과 사례를 작성했다.
- 사례 상태를 `확정`, `설계 계약`, `정답 보류`로 구분하고 Phase 1 착수 전에 해제해야 할 사례를 명시했다.
- 날짜·윤년 산술, 계산 사례 산술, 사례 ID 중복, 질문 ID 정의·참조, Phase 2 이후 파일 수정 지시 부재를 교차 검토했다.
- 기존 `index.html`에는 차이가 없음을 확인해 문서 작성 외 코드 변경이 없도록 유지했다.
- 사용자 답변에 따라 Phase 1의 PVP 원천, 조직 전파, 동률, 일요일, 레벨, 루트, 기술 스택 결정을 기술 설계에 기록했다.
- `202407A SRM.xlsx`의 `PVP+좌+우` 연쇄 수식을 확인하여 날짜별 하위 신규 실적이 상위 경로에 한 번씩 반영되고 이월 잔액은 신규 실적으로 재전파되지 않는 계약을 확정했다.
- Phase 1의 15개 `정답 보류` 사례를 단일 기대 결과의 `확정` 사례로 변경하고 로드맵과 실행 계획의 상태를 갱신했다.

## Decisions

- Phase 1은 자동 최적화나 제품 UI가 아니라 동일 입력에 동일 결과를 내는 순수 계산 코어로 한정했다. 두 장부와 조직 전파의 정확성이 이후 모든 기능의 전제이기 때문이다.
- 일일 장부의 상태와 보름 장부의 원본 누계를 별도 타입과 결과로 유지하고, 일일 초기화가 날짜별 원본을 덮어쓰지 않도록 했다.
- 계획·확정·실제 리비전은 계산 코어가 아니라 후속 애플리케이션 계층의 책임으로 분리했다.
- 최적화용 초과 소멸량 수식은 Phase 1 필수 출력에서 제외하고 Phase 4 결정 사항으로 남겼다.
- 입력 fingerprint는 재현 수단으로 사용하지 않고 Phase 6의 무결성·캐시 기능으로 미뤘다. 재현에는 전체 입력과 당시 RuleSet 본문을 보존하도록 했다.
- 개별 입력뿐 아니라 조직 합계, 이월, 보름 누계와 후속 최적화 점수도 정확한 정수 범위를 검사하도록 설계했다.
- Phase 1 기술 스택은 TypeScript, Vite, Vitest로 확정하고 UI 프레임워크는 Phase 2 전에 결정하기로 했다.
- 보름 작은 쪽에는 보름 PVP 시작값과 신규 PVP 전액을 적용하며 목표 초과분도 자르지 않는다. 일일 PVP 시작잔액은 제외하고 보름 PVP 시작값은 상위 조직에 재전파하지 않는다.
- 날짜별 하위 신규 원본은 발생일에 모든 상위 회원에게 경로당 한 번 연쇄 반영하고 시작·이월 잔액과 정산 결과는 재전파하지 않는다.
- 좌·우 동률은 왼쪽 적용, 일요일은 신규 입력 없는 `SKIP_NO_INPUT`, 프로젝트 조직은 루트 한 명의 연결된 비순환 트리로 확정했다.
- 레벨은 1 이상의 정수로 허용하고 레벨 1 목표 2,400, 레벨 2 목표 1,500, 레벨 3 이상 목표 700, 레벨 4 이상 약 8회 분산 선호로 확정했다.

## Current Status

- 요청된 네 문서의 작성과 Phase 1 계산 계약 정합성 검토가 완료되었다.
- 전체 Phase와 최종 배포 경로가 로드맵에 포함되어 있다.
- Phase 1의 파일 범위, 작업 순서, 테스트 사례, 완료 기준이 구체화되어 있다.
- Phase 1 계산 질문은 모두 확정되었고 이후 Phase 질문만 `정답 보류`로 남아 있다.
- 실행 코드, 패키지 설정, UI, 테스트 코드는 작성하지 않았다.
- 문서 구조·산술 검증만 수행했으며 빌드나 애플리케이션 테스트는 대상이 없어서 실행하지 않았다.

## Next Steps

- Phase 1 구현을 연결할 GitHub Issue를 정한 뒤에만 코드 작업을 시작한다.
- 구현 시 타입 검사, 정답 사례 테스트, 커버리지, 프로덕션 빌드를 실행하고 해당 날짜 개발 로그와 Issue 연결 커밋을 남긴다.
# 추가 기록

- `AGENTS.md`에 GitHub Issue 처리 규칙을 추가했다. 구현 작업은 에이전트가 관련 Issue를 직접 찾고, 없으면 만든다. J님에게 Issue 번호를 기억하거나 제공하라고 요청하지 않는다.
- 일반 커밋에는 `Refs #번호`, Issue 전체를 끝내는 PR에는 `Closes #번호`를 사용한다.

---

## Phase 1 구현 기록

### Work Summary

GitHub Issue #1에 연결해 Phase 1 결정론적 계산 코어를 구현했다. UI·저장소·네트워크와 분리된 TypeScript 순수 함수로 조직 원본 전파, 일일 정산, 보름 누계와 최종 목표 평가를 조합했으며, 문서의 Phase 1 확정·설계 계약 사례 50개를 자동 테스트와 연결했다.

### Work Completed

- TypeScript 7, Vite 8, Vitest 4 기반의 엄격한 타입 검사, 테스트, 커버리지, 빌드 명령과 GitHub Actions CI를 구성했다.
- PV의 0 이상 안전 정수 검사와 모든 조직·이월·보름 합계의 범위 검사를 공통 연산으로 구현했다.
- 반월 날짜, 윤년·월말, 일요일 `SKIP_NO_INPUT` 정책을 현재 시각과 로컬 시간대에 의존하지 않도록 구현했다.
- 회원·배치·시작값·완전한 날짜별 셀을 사전 검증하고 중복 키·ID·슬롯, 부모 누락, 순환, 복수 루트, 비연결 조직, 연결 방향 직접 입력을 위치가 있는 오류로 거부하도록 했다.
- 날짜별 직접 원본만 후위 순서로 상위에 한 번 전파하고, 일일 이월·PVP 적용·커미션 결과가 부모 원본으로 재전파되지 않도록 분리했다.
- 일일 장부에서 PVP를 작은 쪽에 전량 적용하고 공식 최고 단계 하나를 선택하며, 발생 시 전량 초기화하고 미발생 시 PVP·좌·우 항목을 그대로 이월하도록 구현했다.
- 일일 초기화와 독립된 보름 원본 누계, 개인 PVP 목표, 보름 마감 PVP 적용, 좌·우 2,500 목표, 레벨 4 이상 8회 권장 상태를 구현했다.
- 전체 반월 계산 결과에 입력 스냅샷, 날짜·회원별 원본, 일일 정산, 진행 누계, 최종 평가, 규칙·엔진 버전을 포함했다.
- `CALCULATION_CASES.md`의 Phase 1 대상 50개 사례 ID가 실제 테스트 파일에 존재하는지 자동 대조하는 추적성 테스트를 추가했다.
- 독립 코드 리뷰에서 발견한 검증 후 입력 재읽기, `__proto__` 회원 키의 객체 맵 충돌, 같은 버전 문자열을 사용한 RuleSet 본문 변조를 수정했다. 보름 PVP 누계 범위 오류에 발생 날짜를 추가하고 오류 정렬을 로캘 비의존 방식으로 바꿨다.
- 일요일 결과의 달력 모드와 정산 상태를 `businessCalendarMode=SKIP_NO_INPUT`, `settlementStatus=SKIPPED`로 분리하고 계산 사례 문구를 기술 설계와 일치시켰다.
- 기존 Hello World는 제품 기능이 아니므로 제품 UI 없이 Vite 최소 진입점으로 교체했다.

### Decisions

- 정규 입력은 모든 반월 날짜와 회원의 완전한 셀 집합으로 받고, `SELF` 방향은 0을 포함한 필드를 요구하며 `CHILD` 방향은 필드가 존재하는 것 자체를 오류로 처리했다. 계산 중 누락값을 임의로 0으로 해석하지 않기 위해서다.
- 계산 전에 입력을 평범한 정규 스냅샷으로 한 번 복사하고 그 동일한 값을 검증과 계산에 사용한다. 접근자나 변경 가능한 객체가 검증 후 다른 값을 제공하는 시점 차이를 막기 위한 결정이다.
- 규칙 버전 `1.0.0`은 버전 문자열뿐 아니라 공식 단계, 목표와 정책 본문 전체가 정본과 일치할 때만 허용한다. 결과가 같은 버전으로 서로 다르게 재현되는 상황을 차단하기 위해서다.
- 회원 키는 임의의 비어 있지 않은 문자열을 지원하므로 회원별 감사 결과는 null-prototype 레코드에 저장했다. `__proto__` 같은 키도 직렬화에서 사라지지 않는다.
- `SKIP_NO_INPUT`은 영업 달력 모드, `SKIPPED`는 정산 실행 상태로 분리했다. 휴무 원인과 실제 정산 여부를 서로 다른 감사 축으로 유지하기 위해서다.
- 자동 최적화, 완성형 UI, 계획·실제 리비전, 저장 기능은 Phase 1 공개 코어에 포함하지 않았다.

### Current Status

- `npm run typecheck`가 오류 없이 통과한다.
- `npm test -- --run`에서 8개 파일, 167개 테스트가 통과한다.
- `npm run test:coverage`가 통과하며 전체 분기 98.26%, 도메인 분기 98.84%, 엔진 분기 96.51%다.
- `npm run build`로 Vite 프로덕션 빌드가 성공한다.
- Phase 1 계산 사례 50개가 모두 테스트 ID와 연결되어 있으며 알려진 Phase 1 차단 문제는 없다.
- 자동 최적화·완성형 UI·영구 저장은 의도적으로 구현하지 않았다.

### Next Steps

- Issue #1을 닫는 PR에서 구현과 계산 계약을 검토하고 CI 결과를 확인한다.
- Phase 2 착수 전에 Q-PRODUCT-02의 조직·시작값 변경 허용 시점과 UI 프레임워크를 확정한다.

## Work Summary - GitHub Pages 자동 배포 기반

GitHub Pages가 Vite 소스를 그대로 제공해 `/src/main.ts` 요청이 실패하던 배포 구조를 정리했다. `main`에 push된 소스를 GitHub Actions가 빌드하고 `dist`만 Pages에 배포하는 단일 절차를 구성했다.

## Work Completed - GitHub Pages 자동 배포 기반

- Vite의 배포 base를 프로젝트 Pages 경로인 `/ngplan/`로 설정했다.
- `main` push와 수동 실행을 지원하는 GitHub Pages 배포 워크플로를 추가했다.
- 워크플로가 Node.js 24에서 `npm ci`, `npm run build`를 실행하고 `dist`를 Pages 아티팩트로 배포하도록 구성했다.
- 타입 검사, 167개 테스트, 프로덕션 빌드를 실행했다.
- 빌드된 `dist/index.html`의 자산 경로가 `/ngplan/assets/...`로 생성되는 것을 확인했다.

## Decisions - GitHub Pages 자동 배포 기반

- `dist`를 Git에 커밋하지 않고 GitHub Actions 아티팩트로만 배포한다. 생성물을 소스 이력과 분리하고 매번 같은 빌드 절차를 재현하기 위해서다.
- GitHub Pages 저장소 설정의 Source 전환은 J님이 직접 수행하고, 이번 변경에서는 워크플로와 Vite 경로만 준비했다.
- Phase 2 UI는 아직 구현하지 않으므로 배포 후 화면이 비어 보이는 문제는 다음 작업에서 최소 상태 화면과 함께 다룬다.

## Current Status - GitHub Pages 자동 배포 기반

- `npm run typecheck`, `npm test -- --run`, `npm run build`가 모두 통과한다.
- Pages Source를 GitHub Actions로 전환하면 이후 `main` push마다 자동 배포할 준비가 완료되었다.
- 현재 Phase 1 진입점은 화면 콘텐츠를 렌더링하지 않으므로 자동 배포 성공과 화면 구현은 별도 상태다.

## Next Steps - GitHub Pages 자동 배포 기반

- 저장소 Settings → Pages에서 Source를 GitHub Actions로 전환한다.
- 다음 작업에서 사용자가 확인할 수 있는 최소 상태 화면을 구현한다.

## Work Summary - Phase 2 실행 계획

Phase 1 계산 코어 다음 단계인 프로젝트·조직 입력 화면을 구현할 수 있도록 `docs/plans/PHASE_2_EXECUTION_PLAN.md`를 영어 정본으로 작성했다. 첫 한국어 초안에서 개발 용어가 모호하게 번역될 가능성을 확인했기 때문에 구현 계약과 조직 구조 용어는 영어로 고정하고, 제품 화면의 사용자 문구만 한국어로 제공하기로 했다. 애플리케이션 코드와 설정은 변경하지 않았다.

## Work Completed - Phase 2 실행 계획

- 반월 프로젝트 기간, 회원 정보, 부모 기준 좌·우 배치, 네 종류의 시작값, 피라미드 시각화, 오류 피드백을 Phase 2 포함 범위로 정리했다.
- 날짜별 작업표, 계산 결과, 자동 최적화, 확정·실제 리비전, 종료, 영구 저장과 이전 프로젝트 자동 이월을 제외 범위로 명시했다.
- 불완전한 문자열 기반 Draft를 검증한 뒤에만 인메모리 `PlanProject + OrganizationSnapshotInput` 전달 객체로 정규화하는 계층과 파일 구조를 설계했다.
- 기존 `validatePlan`에 가짜 배정표를 넣지 않고 기간 및 조직·시작값 전용 검증 계약을 추출하여 Phase 1과 재사용하도록 계획했다.
- UI 도구, 프로젝트 초안, 회원 카드·좌우 추가 버튼, 시작값 확인, 조직 재배치, 정규 전달 객체, 사용성, 회귀 검증의 작업 패키지와 선행 조건을 작성했다.
- 새 회원의 네 시작값을 모두 0으로 초기화하되 회사 시스템 확인 여부는 별도 상태로 두는 계약과 자동 사례를 추가했다.
- 중간 회원이 빠져도 남은 하위 조직 데이터를 삭제하지 않고 `descendant subtree`를 보존하여 `reassignment queue`에서 다시 연결하는 비파괴적 조직 수정 흐름을 정의했다.
- 자식이 한 명이면 명시적인 승격 또는 분리를 제공하고, 두 명이면 앱이 임의 선택하지 않고 두 서브트리를 모두 재배치하도록 사례를 나눴다.
- 드래그 앤 드롭 대신 각 회원 카드 아래의 좌·우 `+` 버튼으로 신규 회원 생성과 기존 서브트리 재연결을 수행하도록 정했다.
- 가로로 넓어지는 조직은 전용 스크롤 영역과 가지 접기·펼치기로 다루도록 했다.
- ngcatalogue에서 확인한 아토미 계열 청록색·흰 패널·옅은 회색 배경 토큰과 읽기 쉬운 진한 실행 버튼 색상을 UI 정본으로 포함했다.

## Decisions - Phase 2 실행 계획

- Phase 2의 완료 결과를 완전한 `CalculatePlanInput`이 아니라 Phase 3에서 날짜별 배정값을 붙일 수 있는 인메모리 `PlanProject`와 계산용 조직 입력의 전달 객체로 정의했다.
- UI 초안은 문자열과 불완전한 배치를 허용하되 Phase 1의 정본 타입으로 강제 변환하지 않으며, `ERROR`가 있으면 부분 전달 객체를 반환하지 않고 `WARNING`은 보존하도록 했다.
- 연결의 권위 원천은 계속 하위 회원의 `parentMemberKey + sideAtParent` 하나로 유지하고 자식 인덱스와 `CHILD/SELF`는 화면에서 파생하도록 했다.
- Phase 2에서는 인메모리 `IN_PROGRESS` 편집만 계획하고 확정·실제·종료·리비전·영구 저장 정책은 후속 Phase로 남겼다.
- UI 프레임워크와 DOM 테스트는 React + React DOM + Vitest 계열로 확정했으며 기술 선택을 J님에게 요구하지 않도록 문서에 명시했다.
- 시작값은 빈칸이 아니라 0으로 채우고, 확인하지 않은 기본값이 실수로 계산에 들어가지 않도록 회원별 `openingStateConfirmed`를 별도로 요구한다.
- 일반적인 중간 회원 이탈은 재귀 삭제가 아니라 `exclude member`로 처리하고, 살아 있는 직계 자식의 전체 서브트리는 연결만 끊어 보존한다.
- 계획표 생성 뒤 조직이 바뀌는 실제 업무 흐름은 Phase 5에서 새 조직 스냅샷과 재계산으로 처리하되, Phase 2부터 안정적인 회원 키와 비파괴적 조직 명령을 사용하도록 했다.
- 회원 배치는 좌·우 `+`와 명시적 선택으로 처리하고 드래그 앤 드롭은 사용하지 않는다.
- UI는 ngcatalogue/아토미 계열 색상 토큰을 중앙 관리하며 밝은 청록색은 장식, 더 진한 `#087ea4`는 주요 동작과 작은 글자에 사용한다.
- 입력 완료 뒤 Draft가 바뀌면 준비 상태와 활성 전달 객체를 즉시 해제하고 재검증하도록 했으며, 기간 변경과 사용자 수정 제목의 관계도 진입 결정으로 구체화했다.
- 빌드 산출물의 `/ngplan/` 자산 경로는 자동 `smoke:dist`로 검사하고 실제 렌더링은 수동 preview로 구분해 검증하도록 했다.

## Current Status - Phase 2 실행 계획

- Phase 2의 성공 조건, 포함·제외 범위, 계층 경계, 목표 파일, 작업 순서, 테스트와 완료 정의가 작성되었다.
- `ROADMAP.md`의 Phase 2 상태는 구현 전이므로 `대기`를 유지한다.
- Phase 2 구현에 필요한 기술·제품·UI 기본 정책은 영어 실행 계획에서 확정되었다.
- 기존 한국어 계획서는 `docs/plans/PHASE_2_EXECUTION_PLAN_old_ko.md`에 참고용으로 남아 있고, 영어 파일만 구현 정본으로 사용한다.
- 문서만 변경했으므로 타입 검사, 테스트, 커버리지 검사와 빌드는 실행하지 않았다.

## Next Steps - Phase 2 실행 계획

- PRE-WP0에서 영어 계획의 확정 결정을 `TECHNICAL_DESIGN.md`와 `ROADMAP.md`에 동기화한다.
- WP1에서 React 기반 화면 셸과 중앙 색상 토큰부터 구현한다.
- WP2 이후 0 기본값·확인 상태, 좌우 `+` 카드 입력, 비파괴적 회원 제외·서브트리 재배치를 순서대로 구현한다.

## Work Summary - Phase 2 프로젝트·조직 설정 구현

Phase 2 실행 계획과 요구사항 v2, 기술 설계, 로드맵, 계산 정답 사례 및 기존 Phase 1 공개 계약을 원문으로 다시 확인한 뒤 프로젝트·조직 설정 화면을 구현했다. 편집 중에는 불완전한 문자열과 임시 미배치 상태를 허용하되, Phase 3에 전달할 때는 Phase 1 검증을 통과한 불변 `ProjectSetupBundle`만 게시하도록 계층을 분리했다. React 카드 UI에서 루트와 다단계 좌·우 조직을 만들고, 중간 회원 제외 후 살아 있는 후손 서브트리를 재입력 없이 보존·재연결할 수 있다.

## Work Completed - Phase 2 프로젝트·조직 설정 구현

- React와 React DOM으로 `/ngplan/` 앱 셸, 프로젝트 기간·제목 폼, 조직 트리, 회원·시작값 폼, 검증 요약과 설정 번들 요약을 구현했다.
- 새 프로젝트마다 새 프로젝트·조직 스냅샷 ID를 만들고, 이전 회원·연결·시작값을 복사하지 않도록 했다. 기간 기반 제목은 직접 수정하기 전까지만 자동 갱신한다.
- 회원별 보름 PVP, 일일 PVP, 일일 좌·우 네 시작값을 독립 문자열 `'0'`으로 초기화하고 회사 시스템 확인 여부를 별도 boolean으로 관리했다.
- 연결의 권위 원천을 `parentMemberKey + sideAtParent` 하나로 유지하고 자식 인덱스, `SELF/CHILD`, 순회와 재배치 대기열을 파생했다.
- 루트·좌·우 회원 추가, 서브트리 연결·이동·분리·루트 지정과 회원 제외를 원자적 명령으로 구현했다. 실패한 명령은 원본 초안 객체를 그대로 반환한다.
- 무자식 제외, 한 자식의 명시적 승격 또는 분리, 두 자식의 자동 선택 없는 대기열 이동, 루트 제외를 구분했다. 직계 자식 아래의 모든 내부 연결과 회원 데이터는 보존한다.
- `validatePeriod`와 `validateOrganizationSnapshot`을 Phase 1 공개 API로 추출하고 기존 `validatePlan`과 같은 내부 검증 구현을 사용하도록 했다. 기존 오류 코드·위치·정렬·메시지와 계산 의미는 유지했다.
- 빈 문자열을 0으로 바꾸지 않는 명시적 숫자 파싱, 활성 회원만의 정규 입력 생성, 미확인 시작값·미배치 서브트리 차단, 경고 비차단, 부분 번들 금지와 깊은 동결을 구현했다.
- `__proto__` 같은 특수 회원 키도 안전하게 보존하는 시작값 레코드를 만들고, 제외 회원은 정규 회원·시작값 양쪽에서 함께 제외했다.
- 오류 요약에서 해당 프로젝트 필드·회원 필드·카드·슬롯·대기열로 초점을 이동하도록 연결했다. 트리 가로 스크롤, 가지 접기, 키보드 동작, 대화상자 초점 순환·복귀와 좁은 화면 세로 배치를 구현했다.
- 승인된 아토미/ngcatalogue 색상 토큰을 `src/ui/theme.css`에 중앙화하고 상태를 색상뿐 아니라 텍스트로도 표시했다.
- React/jsdom 테스트와 계층별 커버리지 게이트, `preview`, `/ngplan/` 배포 산출물 스모크 검사 및 CI·Pages 검증 단계를 추가했다.
- 자동 검증에서 타입 검사, 13개 파일의 285개 테스트, 커버리지, 프로덕션 빌드와 `smoke:dist`가 모두 통과했다. 전체 커버리지는 문장 98.34%, 분기 95.56%, 함수 100%, 줄 98.30%였다. 애플리케이션 분기는 96.97%, UI 집계 분기는 86.53%로 각 계층 기준을 통과했다.
- 인앱 브라우저의 실제 `/ngplan/` 미리보기에서 루트와 좌·우 회원 생성, 0 기본값의 미확인 차단, 중간 회원 제외, 후손 서브트리 대기열 표시·재연결, READY 재게시를 확인했다. 브라우저 콘솔 오류·경고는 없었다.

## Decisions - Phase 2 프로젝트·조직 설정 구현

- 불완전한 UI 상태를 Phase 1 타입으로 단언하지 않고 별도 `ProjectSetupDraft`로 유지했다. 편집 편의성과 계산 계약의 엄격함을 동시에 지키기 위해서다.
- 날짜별 0 배정 셀을 만들거나 `calculatePlan`을 호출해 준비 상태를 판정하지 않았다. Phase 2는 기간·조직·시작값만 책임지고 날짜별 계획은 Phase 3이 맡는다.
- 시작값 0과 회사 시스템에서 확인한 0을 구분했다. 반복 입력을 줄이면서 확인하지 않은 기본값이 계산에 조용히 들어가는 위험을 막기 위해서다.
- 일반 회원 이탈을 재귀 삭제로 처리하지 않고 해당 회원만 제외한 뒤 직계 자식 서브트리를 보존했다. 두 자식 중 새 위치는 업무 판단이 필요하므로 앱이 자동 결정하지 않는다.
- 게시된 묶음은 깊게 동결하고, 의미 있는 초안 변경은 활성 묶음을 즉시 해제하도록 했다. Phase 3이 오래된 조직이나 시작값을 소비하지 않게 하기 위해서다.
- Phase 2에는 라우터, 전역 상태, 드래그앤드롭과 브라우저 저장소를 추가하지 않았다. 현재 범위의 명시적 버튼 흐름과 인메모리 정책에 필요한 최소 의존성만 사용했다.
- 계획표 생성 뒤 조직 변경과 기존 리비전의 관계는 Phase 5, 저장·복구·종료 잠금은 Phase 6으로 유지했다.

## Current Status - Phase 2 프로젝트·조직 설정 구현

- Phase 2 실행 계획의 프로젝트, 회원, 카드형 토폴로지, 비파괴적 제외·재배치, 검증·정규화, 접근성 기준과 배포 스모크가 구현되었다.
- `npm run typecheck`, `npm test -- --run`, `npm run test:coverage`, `npm run build`, `npm run smoke:dist`가 모두 통과한다.
- 프로덕션 빌드는 `/ngplan/assets/` 아래 JavaScript와 CSS를 참조하며 스모크 검사가 실제 파일 존재를 확인한다.
- Phase 2에는 의도적으로 영구 저장이 없으므로 새로고침·브라우저 종료 시 초안이 사라진다. 이 제한은 화면 상단에 표시된다.
- 날짜별 계획표, 계산 결과, 최적화, 확정·실제 리비전, 종료와 내보내기는 아직 구현하지 않았다.

## Next Steps - Phase 2 프로젝트·조직 설정 구현

- Phase 3은 React 초안이 아니라 활성 불변 `ProjectSetupBundle`만 받아 날짜·회원별 수동 계획 작업표를 만든다.
- Phase 5에서 계획 이후 회원 이탈·조직 변경 시 새 조직 스냅샷과 기존 계획·확정·실제 리비전의 재계산 관계를 확정한다.
- Phase 6에서 저장 방식, 새로고침 복구, `CLOSED`와 읽기 전용 종료 기록을 구현한다.
