# 2026-07-17 ngplan Development Log

## Work Summary

과거 실사용 SRM 작업표의 회원 열 배치를 다시 검토해 오른쪽 조직을 통째로 뒤집는 기존 규칙을 먼저 전체 중위 순회로 교체했다. 이후 민경욱 202407A 자료에서 표지판 아래 무표지 회원인 `Han/Yona`, `Simone/Raniton`의 순서가 여전히 반대임을 확인했다. 원본 수식을 다시 추적해, 왼쪽 조직은 표준 중위 순회를 유지하고 오른쪽 조직은 번호 표지판 회원을 기준점으로 두면서 그 기준점의 무표지 오른쪽 하위 묶음만 바깥 방향으로 펼치도록 보완했다.

## Work Completed

* 1차 수정에서 수동 계획 스키마의 회원 순서를 루트의 좌우 구분 없이 전체 조직 표준 중위 순회로 변경했다.
* 오른쪽 회원의 왼쪽 하위 회원이 부모보다 먼저, 오른쪽 하위 회원이 부모보다 나중에 표시되는 회귀 테스트를 추가했다.
* 기존 거울 순서로 저장된 수동 계획을 앱 시작 시 날짜와 회원 키로 재조립해 입력값을 보존하고 새 열 순서에 맞추도록 했다.
* 복원된 과거 계획에서 회원별 값이 유지되고 해당 셀을 계속 편집할 수 있는 통합 테스트를 추가했다.
* 1차 수정에서 Phase 3 실행 계획과 정본 요구사항 v2의 거울·역중위 표현을 전체 조직 중위 순회 계약으로 수정했다.
* 31명 완전 이진트리 규모 테스트의 루트 다음 회원 기대값을 새 순서에 맞게 수정했다.
* 민경욱 202407A 원본에서 `유리 → Han → Yona → Kelly → Simone → Raniton`으로 이어지는 수식 관계와 열 순서를 대조했다.
* 오른쪽 조직의 번호 표지판 회원은 실제 좌·우 위치를 유지하고, 표지판의 무표지 오른쪽 하위 묶음만 역중위 순회하도록 회원 열 파생 함수를 보완했다.
* `박진숙 → 남승우 → 유리 → Han → Yona → Kelly → Simone → Raniton` 순서를 고정하는 회귀 테스트를 추가했다.
* 브라우저 플러그인으로 동일한 9명 조직을 화면에서 직접 만들고 수동 계획표를 열어 `민경욱 → 박진숙 → 남승우 → 유리 → Han → Yona → Kelly → Simone → Raniton` 열 순서를 확인했다.
* Phase 3 실행 계획과 정본 요구사항 v2를 표지판 기준 하이브리드 순회 계약으로 다시 동기화했다.
* 전체 556개 테스트, TypeScript 타입 검사, 프로덕션 빌드, 배포 산출물 smoke 검사를 실행했다.

## Decisions

* 전체 표준 중위 순회만으로는 오른쪽 표지판 아래의 무표지 4레벨 이하 묶음을 원본 SRM 순서로 펼칠 수 없었다. 왼쪽 조직은 표준 중위 순회를 유지하고, 오른쪽 조직의 번호 표지판을 기준점으로 삼아 무표지 왼쪽 묶음은 중위, 무표지 오른쪽 묶음은 역중위로 순회한다.
* 번호 표지판 회원을 만날 때마다 기준점 규칙을 다시 시작한다. 이로써 `2·3·4번` 자체를 거울 반전했던 이전 오류를 반복하지 않으면서 그 아래 무표지 회원만 바깥 방향으로 펼칠 수 있다.
* 오른쪽 최상단 회원에 표지판이 없으면 표준 중위 순회를 사용한다. 표지판이 없는 일반 조직의 기존 표시 동작과 규모 테스트를 보존하기 위한 안전한 기본값이다.
* 맨 위 회원은 왼쪽 조직과 오른쪽 조직의 논리적 경계에 두되, 양쪽 회원 수가 다를 때 기하학적 중앙을 만들기 위한 반전이나 빈 열 보정은 하지 않는다.
* 화면 표시 순서만 변경하고 Phase 4의 root-first·LEFT-before-RIGHT 비즈니스 정본 순서는 유지했다. 자동 계획 지문·목적함수·타이브레이커와 UI 배치 계약은 계속 분리한다.
* 저장 형식 버전은 올리지 않았다. 기존 셀은 안정적인 날짜·회원 키를 갖고 있고, 기존 재조립 함수로 값 손실 없이 현재 스키마 순서로 바꿀 수 있기 때문이다.
* 2026-07-13 개발 로그의 거울 규칙 기록은 당시 결정의 이력으로 보존하고, 이번 변경 사유를 오늘 로그에 새로 남겼다.

## Current Status

민경욱 202407A의 문제 구간이 `박진숙 → 남승우 → 유리 → Han → Yona → Kelly → Simone → Raniton` 순서로 생성된다. 브라우저 화면 재현, 전체 556개 테스트, 타입 검사, 빌드와 dist smoke 검사가 모두 통과했다. 기존 계획은 복원 시 회원 키로 값을 보존한 채 새 열 순서로 정규화되며, Phase 4 자동 계획의 비즈니스 회원 순서에는 변경이 없다.

## Next Steps

* J님 브라우저에 저장된 실제 17명 민경욱 프로젝트를 새 코드로 다시 열어 전체 열 순서를 최종 확인한다.
* 현재 앱의 표지판은 1~4번까지다. 과거 파일의 5번 표지판까지 제품 기능으로 지원할 필요가 생기면 데이터 타입과 표시 계약을 함께 확장한다.
