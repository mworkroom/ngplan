# 2026-08-01 ngplan Development Log

## Work Summary

두 작업이 같은 작업 폴더에서 동시에 진행된 뒤 전체 변경을 통합 검토했다. 회원별 좌·우 목표 규칙과 이전 내용 화면 개선은 함께 정상적으로 유지되고 있었으며, 안전 저장을 가리키는 일부 화면 문구만 새 용어와 달라 이를 통일했다.

## Work Completed

- 대상 Codex 작업과 현재 Git 변경을 대조해 같은 파일에 합쳐진 프로젝트 제목, 이전 내용 화면, 회원별 좌·우 목표, 저장 형식 이전이 모두 유지되는지 확인했다.
- 회원 삭제 및 자동 계산 적용 확인창에 남아 있던 `보관본`, `사본으로 열기`를 `현재 내용 저장`, `이전 내용 보기`, `새 계획 만들기`로 바꿨다.
- 전체 회귀 검사에서 이전 문구를 기대하던 UI 테스트 1개가 실패해 새 표현을 검증하도록 함께 갱신했다.
- 충돌 표식, 구형 엔진·클라우드 문서 타입, 변경 파일 내 비밀정보와 불필요한 생성 파일이 없는지 확인했다.
- 전체 테스트와 커버리지, TypeScript 검사, 프로덕션 빌드, 배포 파일 스모크 검사를 실행했다.

## Decisions

- 사용자에게 같은 기능을 여러 용어로 설명하지 않도록 안전 저장과 복구 기능은 모든 화면에서 `이전 내용`이라는 표현을 사용한다.
- 새 기간을 만드는 경우에는 회원별 좌·우 목표를 2,500으로 초기화하고, 같은 기간의 이전 내용으로 새 계획을 만드는 경우에는 당시 목표값을 보존한다.
- GitHub Pages 배포가 `main` 푸시에 연결되어 있으므로 최종 통합 변경은 검증 후 `main`에 직접 반영한다.

## Current Status

- 통합 코드 검토와 자동 검증을 완료했다.
- `AGENTS.md`는 프로젝트의 사용자·UI 원칙을 담은 파일로 최종 커밋에 포함한다.
- 로컬 전용 환경 변수, 의존성 폴더, 커버리지 결과와 빌드 결과는 Git에서 제외한다.

## Next Steps

- 최종 커밋을 `main`에 푸시하고 GitHub Actions의 CI와 Pages 배포가 성공하는지 확인한다.
- 배포 완료 후 공개 주소에서 최신 자산과 기본 화면 응답을 확인한다.

## Work Summary - 브라우저 뒤로 가기 목록 이동

엄마용 앱 목록에서 ngplan을 연 뒤 회원 등록 또는 플랜 화면에서 브라우저의 뒤로 가기를 누르면, ngplan의 계획 목록을 건너뛰고 `www.nangok.app`으로 돌아가는 문제를 수정했다.

## Work Completed - 브라우저 뒤로 가기 목록 이동

- 계획 목록에서 회원 등록·플랜 편집 화면을 열 때 브라우저 방문 기록을 한 단계 추가하도록 변경했다.
- 편집 화면에서 브라우저 뒤로 가기를 누르면 ngplan 안의 전체 목록을 표시하고 최신 목록을 다시 불러오도록 연결했다.
- 화면의 `전체 목록으로` 버튼도 같은 방문 기록을 사용해, 버튼과 브라우저 뒤로 가기의 결과가 일치하도록 만들었다.
- 회원 등록 화면에서 브라우저 뒤로 가기를 실행해 전체 목록이 표시되고 `www.nangok.app`으로 이동하지 않는 회귀 테스트를 추가했다.

## Decisions - 브라우저 뒤로 가기 목록 이동

- 브라우저 주소는 새 경로를 만들지 않고 현재 ngplan 주소를 유지한다. 화면 상태만 방문 기록에 남겨 기존 배포 주소와 로그인 흐름을 바꾸지 않는다.
- `전체 목록으로` 버튼은 현재 계획을 먼저 저장한 뒤 브라우저 기록으로 돌아간다. 브라우저 뒤로 가기는 편집 화면이 정리될 때 기존 자동 저장 절차가 계속 작동한다.

## Current Status - 브라우저 뒤로 가기 목록 이동

- 코드와 회귀 테스트를 작성했다.
- CloudApp 관련 테스트 21개, 전체 테스트 687개, TypeScript 검사, 프로덕션 빌드와 배포 파일 스모크 검사가 통과했다.
- 현재 배포본에서 `www.nangok.app → ngplan → 회원 등록 → 전체 목록으로` 흐름이 정상임을 확인했고, 새 코드는 브라우저 뒤로 가기까지 같은 목록 화면으로 연결하도록 검증했다.

## Next Steps - 브라우저 뒤로 가기 목록 이동

- J의 요청에 따라 커밋·푸시·배포 여부를 결정한다.
- 배포 후 실제 주소에서 `전체 목록 → 회원 등록 → 브라우저 뒤로 가기 → 전체 목록` 흐름을 다시 확인한다.

## Work Summary - 새 저장 형식 동기화 복구

복구본으로 만든 계획과 최신 계획 수정 내용이 이 기기에는 남지만 Supabase에는 저장되지 않는 문제를 조사했다. 앱은 새 문서 형식인 버전 2를 보내고 있었으나 데이터베이스의 프로젝트·자동 보관·하루 보관 제약조건은 버전 1만 허용해 모든 저장 요청을 거절하고 있었다.

## Work Completed - 새 저장 형식 동기화 복구

- 기존 버전 1 자료를 그대로 유지하면서 프로젝트와 두 종류의 보관본이 문서 버전 1과 2를 모두 받을 수 있는 Supabase 마이그레이션을 추가했다.
- 문서 안의 버전과 별도 버전 번호가 서로 일치하는지도 계속 검사하도록 제약조건을 유지했다.
- 수정 전 문서를 자동 보관할 때 수정 후 버전 번호를 붙이던 복구 보관 함수를 바로잡아, 문서 내용과 버전 번호가 항상 함께 이전 상태를 가리키도록 했다.
- 목록의 `이 기기에 저장됨 · 동기화 대기`를 쉬운 두 문장으로 바꾸고 `다시 저장하기` 버튼을 추가했다.
- 정상적인 짧은 저장 대기는 별도로 노출하지 않고, 실제로 인터넷 저장이 끝나지 않은 계획에만 안내가 보이도록 기존 조건을 유지했다.
- 같은 화면에 남아 있던 `클라우드`, `동기화` 표현을 `인터넷에 저장`이라는 한 가지 표현으로 정리했다.

## Decisions - 새 저장 형식 동기화 복구

- 버전 1 자료를 한꺼번에 변환하지 않는다. 기존 자료는 버전 1로 계속 읽고, 새로 저장되는 자료는 버전 2로 받아 데이터 손상 위험을 줄인다.
- 저장 실패 안내를 없애지는 않는다. 사용자가 내용이 인터넷에 저장되지 않았다는 사실과 이 기기에는 남아 있다는 사실을 알아야 하므로, 기술 용어 대신 결과와 안전 상태를 직접 설명한다.

## Current Status - 새 저장 형식 동기화 복구

- 코드, 화면 문구, 회귀 테스트와 데이터베이스 마이그레이션을 작성했다.
- 운영 Supabase에 두 마이그레이션을 적용했다. 기존 계획 10개는 버전 1 상태로 그대로 남아 있고 문서와 버전 번호가 어긋난 자료는 0개다.
- 기존 계획을 트랜잭션 안에서 버전 2로 저장해 프로젝트·하루 보관·복구 보관이 모두 성공하는지 확인한 뒤 전체 시험 변경을 되돌렸다.
- 관련 화면 테스트 21개, 전체 테스트 687개, TypeScript 검사, 프로덕션 빌드와 배포 파일 스모크 검사가 통과했다.
- 로컬 앱은 브라우저에서 로그인 화면까지 정상 렌더링되고 관련 콘솔 오류가 없었다. 저장 실패 카드는 로그인된 목록에서만 볼 수 있어 실제 카드의 시각 검증은 진행하지 못했고, 문구·버튼·재시도 호출은 DOM 회귀 테스트로 확인했다.
- Supabase 보안·성능 검사에서 이번 마이그레이션으로 새로 생긴 경고는 없었다. 표시된 항목은 적용 전부터 있던 다른 앱과 기존 공용 구조의 경고다.

## Next Steps - 새 저장 형식 동기화 복구

- 같은 기기와 브라우저에서 ngplan을 다시 열어, 저장 대기 계획이 자동으로 인터넷에 저장되고 안내가 사라지는지 확인한다.
- 화면 문구 변경과 마이그레이션 파일을 Git에 반영하려면 기존 브라우저 뒤로 가기 변경과 함께 커밋·푸시·배포한다.
- 배포 후 로그인된 실제 계획 목록에서 실패 안내의 글자 크기와 `다시 저장하기` 버튼 배치를 최종 확인한다.

## Current Status - 통합 배포

- 브라우저 뒤로 가기와 새 저장 형식 동기화 복구를 커밋 `37c83f8`로 `main`에 푸시했다.
- GitHub Actions의 일반 검증과 격리 Supabase 마이그레이션·RLS 검사가 모두 통과했다.
- GitHub Pages 빌드와 배포가 성공했고 `plan.nangok.app`이 새 JavaScript와 CSS 자산을 HTTP 200으로 제공한다.
- 공개 JavaScript에 새 저장 실패 안내와 `다시 저장하기`가 포함되고, 예전 `이 기기에 저장됨 · 동기화 대기` 문구가 빠진 것을 확인했다.
- 공개 로그인 화면이 정상 렌더링되며 브라우저 콘솔 경고와 오류는 없었다.

## Next Steps - 통합 배포

- 같은 기기와 브라우저에서 저장 대기 계획이 자동 저장되어 안내가 사라지는지 확인한다.
- 로그인된 실제 계획 목록에서 실패 안내의 글자 크기와 버튼 배치를 확인한다.
- 실제 브라우저 뒤로 가기가 ngplan의 전체 목록으로 돌아오는지 확인한다.

## Work Summary - 잘못 선택한 기간의 새 계획 생성 방식

실제 운영에서는 반월마다 참여 회원, 배치, 직접 실적을 만드는 회원이 달라 이전 기간의 조직 정보를 재사용하지 않는다. 기존에는 계획표 사용 후 기간을 바꾸면 날짜별 숫자만 제외하고 회원·배치·시작값을 새 기간 계획에 복사했으나, 이 동작이 불필요한 재사용과 사용자 혼란을 만들 수 있어 빈 계획 생성 방식으로 변경했다.

## Work Completed - 잘못 선택한 기간의 새 계획 생성 방식

- 계획표 사용 후 기간을 바꾸면 이전 회원·배치·시작값·날짜별 숫자를 모두 제외하고, 선택한 기간과 화면에서 확인한 프로젝트명만 가진 빈 계획을 새 ID로 만들도록 변경했다.
- 기간을 바꿀 수 없는 이유만 알리지 않고, 원래 계획이 남는다는 점과 아래 버튼으로 빈 계획을 만든 뒤 회원을 다시 입력해야 한다는 다음 행동을 대화상자에 함께 표시했다.
- 버튼을 `새 기간 계획 처음부터 만들기`로 바꾸고 진행 중 문구도 `새 계획 만드는 중…`으로 통일했다.
- 새 기간 계획에 회원이 한 명도 복사되지 않는지, 입력한 숫자가 있는 경우와 계획표만 연 경우 모두 원본을 유지하는지 회귀 테스트를 갱신했다.
- Phase 6A 실행 계획과 기술 설계의 기간 보호 계약을 실제 운영 방식에 맞게 수정했다.

## Decisions - 잘못 선택한 기간의 새 계획 생성 방식

- 기간이 달라지면 날짜별 숫자뿐 아니라 참여 회원과 배치도 재사용하지 않는다. 매 반월의 계획을 독립적인 새 작업으로 취급한다.
- `이전 내용 보기`는 같은 기간의 과거 회원·배치·숫자를 되살리는 용도로만 유지한다. 잘못 선택한 기간을 고치는 다음 행동으로 안내하지 않는다.
- 아직 계획표 사용을 시작하지 않았다면 기존 계획 안에서 기간을 수정할 수 있다. 계획표 사용을 시작했다면 원본 기간을 바꾸지 않고 빈 새 계획을 만든다.

## Current Status - 잘못 선택한 기간의 새 계획 생성 방식

- 기간 보호 함수, 회원 입력 화면 문구, 버튼, 관련 단위·UI 테스트와 설계 문서를 수정했다.
- 관련 테스트 24개와 TypeScript 검사가 먼저 통과했다. 전체 커버리지 첫 실행에서는 새 예외 안내 분기가 빠져 UI statement 기준이 0.02% 부족했고, 빈 계획을 만들 수 없거나 생성에 실패한 경우의 다음 행동·원본 유지 테스트 2개를 추가했다.
- 최종적으로 전체 테스트 689개와 커버리지 기준, TypeScript 검사, 프로덕션 빌드와 배포 파일 스모크 검사가 통과했다.
- 로컬 프로덕션 화면은 HTTP 200, 로그인 화면 렌더링, 빈 화면·프레임워크 오류 없음, 콘솔 경고·오류 없음까지 확인했다. 기간 변경 대화상자는 로그인된 자료가 필요해 운영 자료를 임의로 수정하지 않고 사용자 동작 회귀 테스트로 검증했다.

## Next Steps - 잘못 선택한 기간의 새 계획 생성 방식

- 로그인된 실제 화면에서 안내 문장이 짧게 나뉘어 읽히고, 빈 새 계획에서 회원 입력부터 시작되는지 한 번 확인한다.
- J의 요청이 있으면 검증된 변경을 커밋·푸시·배포한다.

## Work Summary - 회원 입력 변경 시 확인 해제

회원 입력 화면에서 이름을 바꿀 때만 하단 시작값 확인이 풀리던 동작을 고쳤다. 이제 확인 후 입력 내용을 하나라도 수정하면 다시 확인해야 한다.

## Work Completed - 회원 입력 변경 시 확인 해제

- 이름뿐 아니라 ID, 이번 기간 PVP 목표, 좌·우 목표, 이름 강조색을 바꿔도 확인이 풀리도록 변경했다.
- PVP·좌·우 시작값 중 하나를 바꿔도 확인이 풀리도록 변경했다.
- 실제 값이 달라지지 않은 편집과 확인 체크 자체는 기존 상태를 불필요하게 바꾸지 않도록 유지했다.
- 모든 회원 입력 항목과 세 시작값의 확인 해제 동작을 회귀 테스트에 추가했다.

## Decisions - 회원 입력 변경 시 확인 해제

- 하단 확인은 현재 화면에 보이는 회원 정보와 시작값을 검토했다는 뜻으로 취급한다. 따라서 확인 후 관련 입력값이 달라지면 항목 종류와 관계없이 다시 확인받는다.

## Current Status - 회원 입력 변경 시 확인 해제

- 구현과 회귀 테스트를 작성했다.
- 프로젝트 설정 단위 테스트 43개와 TypeScript 전체 검사가 통과했다.

## Next Steps - 회원 입력 변경 시 확인 해제

- J의 요청이 있으면 변경을 커밋·푸시·배포한다.

## Work Summary - 회원 카드 정보 순서 정리

조직도의 회원 카드에서 ID, 목표, 시작값이 서로 섞여 보여 필요한 정보를 빠르게 구분하기 어려웠다. 각 정보를 의미별 한 줄로 나누고 요청한 순서대로 정리했다.

## Work Completed - 회원 카드 정보 순서 정리

- 회원 이름 아래 정보를 `ID`, `목표`, `시작값` 순서로 배치했다.
- 목표는 `PVP 값 | 좌/우 값`, 시작값은 `PVP 값 | 좌 값 | 우 값` 형식으로 한 줄씩 표시했다.
- 카드 정보 줄의 간격과 숫자 정렬을 기존 카드 폭에 맞게 조정했다.
- 카드 UI 테스트가 새 문구와 표시 순서를 확인하도록 갱신했다.

## Decisions - 회원 카드 정보 순서 정리

- 항목 이름을 매 줄 앞에 직접 표시해 이전 화면을 기억하거나 숫자의 의미를 추측하지 않아도 되게 했다.
- 입력한 숫자는 회원 입력 화면과 바로 비교할 수 있도록 별도의 단위나 천 단위 쉼표를 덧붙이지 않는다.

## Current Status - 회원 카드 정보 순서 정리

- 카드 컴포넌트, 스타일과 회귀 테스트를 수정했다.
- 회원 카드 UI 테스트 12개, TypeScript 검사와 프로덕션 빌드가 통과했다.
- 실제 브라우저에서 예시 회원 카드를 렌더링해 세 정보 줄이 모두 한 줄로 표시되고 콘솔 오류가 없는지 확인했다.

## Next Steps - 회원 카드 정보 순서 정리

- J의 요청이 있으면 변경을 커밋·푸시·배포한다.

## Work Summary - 회원 입력창 중복 문구 정리

회원 입력창에서 기간과 시작값이라는 말이 각 라벨에 반복되어, 같은 화면 안에서 의미를 여러 번 읽어야 하는 문제가 있었다. 첨부 화면의 표시 의도에 맞춰 제목은 한 번만 두고 각 입력 라벨을 짧게 정리했다.

## Work Completed - 회원 입력창 중복 문구 정리

- 목표 라벨을 `PVP 목표`, `좌우 목표`로 줄였다.
- 좌우 목표 드롭다운의 선택지를 `2,500 PV`, `1,500 PV`로 줄이고 아래 설명 문구를 제거했다.
- 시작값 입력 라벨을 `PVP`, `좌`, `우`로 줄였다.
- 시작값 입력 3개를 데스크톱과 좁은 화면 모두 한 줄 3칸으로 배치하도록 스타일을 추가했다.
- 기존 라벨을 기대하던 UI 테스트도 새 라벨에 맞게 갱신하고, 소스에서 이전 문구가 남아 있지 않은 것을 읽기 전용으로 확인했다.

## Decisions - 회원 입력창 중복 문구 정리

- `이번 기간 목표`와 `시작값`은 섹션 제목에서만 말한다. 각 필드에는 사용자가 입력할 값의 이름만 표시한다.
- 좌우 목표 선택지에는 이미 섹션과 라벨에서 대상이 드러나므로 `좌/우 각`, `기본` 같은 설명을 반복하지 않는다.
- 세 시작값은 사용자가 서로 비교하며 입력하는 값이므로 화면 폭이 좁아도 세 칸 한 줄을 유지한다.

## Current Status - 회원 입력창 중복 문구 정리

- 입력 컴포넌트, 스타일과 관련 테스트를 수정했다.
- 이전 라벨 6종이 관련 소스와 테스트에 남아 있지 않은 것을 읽기 전용 검사로 확인했다.
- 현재 실행 환경의 터미널 사용량 제한과 로컬 브라우저 주소 차단으로 자동 테스트 및 실제 브라우저 캡처는 재실행하지 못했다.

## Next Steps - 회원 입력창 중복 문구 정리

- J의 환경에서 회원 입력창을 열어 세 시작값이 한 줄로 보이는지 확인한다.
- 실행 제한이 풀리면 UI 테스트, TypeScript 검사와 브라우저 캡처를 다시 실행한다.
