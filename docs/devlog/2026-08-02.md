# 2026-08-02 ngplan 개발 로그

## Work Summary

계획표의 4단 헤더가 페이지를 세로로 내릴 때 사라지는 문제를 다시 확인했습니다. 첫 수정에서 표 안에 세로 스크롤을 추가하자 시트 높이가 줄어 합계 행이 바로 보이지 않는 문제가 생겨, 시트 높이는 유지하면서 헤더만 고정하는 구조로 다시 수정했습니다.

## Work Completed

- 시트의 세로 높이 제한을 제거해 날짜 행과 합계 행이 원래 시트 높이에 모두 포함되도록 복구했습니다.
- 실제 표의 4단 헤더와 같은 고정용 헤더를 시트 위에 겹쳐 배치했습니다. 고정용 헤더는 화면 읽기 도구에서 중복으로 읽히지 않도록 숨겼습니다.
- 페이지를 내리면 고정용 `ID → 목표값 → 잔액 → 날짜` 헤더만 화면 상단에 남고, 원본 표와 합계 행은 원래 문서 흐름을 유지합니다.
- 표를 가로로 움직일 때 원본 표와 고정용 헤더의 가로 위치가 함께 이동하도록 동기화했습니다.
- 임시 브라우저 fixture로 시트 `clientHeight`와 `scrollHeight`가 동일해 합계 행이 잘리지 않는지, 페이지 스크롤 후 헤더가 상단에 고정되는지, 가로 스크롤 위치가 일치하는지 검증한 뒤 fixture는 삭제했습니다.
- 계획표 컴포넌트 테스트 24개, 타입 검사, 프로덕션 빌드를 통과했습니다.
- 전체 커버리지 테스트 첫 실행에서 시각 전용 헤더까지 회원 순서 검사에 포함되는 기존 테스트 1개가 실패했습니다. 실제 표 안의 회원 헤더만 검사하도록 범위를 좁힌 뒤 전체 714개 테스트와 커버리지 기준을 통과했습니다.

## Decisions

기존 표는 가로 스크롤 상자 안에 있어 원본 헤더에 `position: sticky`만 적용하면 페이지 스크롤에 고정되지 않습니다. 표에 세로 스크롤을 추가하면 고정은 되지만 시트가 짧아져 합계 행이 가려졌습니다. 따라서 원본 표의 전체 높이는 그대로 두고, 시각 전용 헤더를 별도로 겹쳐 페이지에 고정하는 방식을 선택했습니다.

## Current Status

로컬 fixture에서 15개 날짜 행과 합계 행이 세로 잘림 없이 전체 시트 높이에 포함되는 것을 확인했습니다. 페이지를 400px 내린 상태에서도 고정 헤더의 상단 위치가 0으로 유지됐고, 가로 스크롤 320px도 원본 표와 고정 헤더에 동일하게 적용됐습니다. 계획표 실제 화면은 로그인 뒤에 있어 회원 데이터가 필요한 화면까지는 접근하지 못했지만, 전체 714개 테스트·커버리지 기준·타입 검사·프로덕션 빌드는 모두 통과했습니다.

## Next Steps

로그인 후 실제 계획표에서 합계 행까지 원래 높이로 보이는지, 페이지를 내린 뒤 고정 헤더와 첫 날짜 열이 기대한 위치에 남는지 한 번 더 확인합니다.

## Work Summary - 계획표 이미지 만들기

계획표를 휴대전화로 촬영하고 사람별 영역을 자르던 작업을 줄이기 위해, 시트에서 최대 5명을 골라 날짜와 해당 회원 열만 PNG 이미지로 만드는 기능을 추가했습니다.

## Work Completed - 계획표 이미지 만들기

- 계획표 헤더의 동작 순서를 `이미지 만들기 → 설정으로 돌아가기 → 전체 목록으로`로 배치했습니다.
- 일반 창 너비에서도 세 버튼이 화면 오른쪽 밖으로 밀리지 않도록 계획표 헤더 너비를 보정했습니다.
- 큰 체크 항목으로 회원을 1명부터 5명까지 고르는 선택창을 추가했습니다.
- 회원을 누른 순서와 관계없이 실제 계획표에 보이는 순서로 정렬해 이미지에 넣도록 했습니다.
- 맨 왼쪽 날짜 열과 선택한 회원별 `PVP·좌·우` 세 열, 저장된 계획 제목을 함께 출력하도록 했습니다.
- 현재 셀 값뿐 아니라 수당 발생 표시와 실제 차이 빨간 표시도 이미지에 남도록 처리했습니다.
- 생성한 이미지를 먼저 확인한 뒤 저장할 수 있게 했고, 파일 공유를 지원하는 기기에서는 `이미지 공유하기`도 표시하도록 했습니다.
- SVG를 Blob 주소로 읽을 때 캔버스 저장이 차단되는 실제 브라우저 오류를 데이터 주소 방식으로 해결했습니다.
- 이미지에 필요한 스타일만 복사하도록 최적화해 최대 5명 이미지 생성 시간을 실제 확인 화면 기준 약 1.8초로 줄였습니다.
- 이미지 선택 순서, 최대 5명 제한, PNG 생성과 실패 처리, 저장·공유, 헤더 버튼 순서 테스트를 추가했습니다.
- 기존 안내 문구 변경과 맞지 않던 앱 테스트 문구를 현재 화면 문구에 맞췄습니다.

## Decisions - 계획표 이미지 만들기

회원 이름마다 개별 버튼을 붙이지 않고 헤더에 이미지 작업의 시작점을 하나만 두기로 했습니다. 한 사람과 여러 사람을 같은 흐름에서 처리할 수 있고, 이름 열에 반복되는 버튼을 만들지 않아 계획표가 복잡해지지 않기 때문입니다. 출력 순서는 선택 순서가 아니라 시트 순서를 기준으로 고정해 전달 이미지가 원본 계획표와 항상 같은 방향으로 읽히게 했습니다. 최대 인원은 실제 사용 범위인 5명으로 제한했고, 5명 결과가 1828×1684px로 충분히 읽을 수 있는 것을 확인했습니다.

## Current Status - 계획표 이미지 만들기

로컬 실제 브라우저에서 `202608A 민경욱` 확인 자료를 열어 회원을 역순으로 선택한 뒤에도 `날짜 → 시트 순서의 회원`으로 출력되는 것을 확인했습니다. 최대 5명 선택 시 여섯 번째 항목이 비활성화되고, 1828×1684px PNG가 약 1.8초에 생성되는 것도 확인했습니다. 타입 검사, 전체 714개 테스트, 커버리지 기준, 프로덕션 빌드, 배포 파일 스모크 검사를 모두 통과했습니다.

## Next Steps - 계획표 이미지 만들기

- 실제 저장된 계획에서 회원 이름, 셀 값, 수당 표시, 빨간 표시가 기대한 모습으로 함께 나오는지 최종 사용 확인을 진행합니다.
- 회원 이름을 눌러 한 사람 이미지를 바로 만드는 단축 동작은 별도 후속 기능으로 검토합니다.

## Work Summary - 화면 높이에 따른 고정 헤더 겹침과 잘림 수정

고정 헤더 높이를 156px로 고정한 영향으로 화면 배율과 실제 글자 높이에 따라 월 초반에는 원본 헤더가 아래로 삐져나오고, 월 후반에는 고정 헤더의 시작값 행이 잘리는 문제를 수정했습니다.

## Work Completed - 화면 높이에 따른 고정 헤더 겹침과 잘림 수정

- 고정 헤더의 `height: 156px`와 원본 표를 당기던 `margin-top: -156px` 규칙을 제거했습니다.
- 고정 헤더와 원본 표를 같은 CSS Grid 칸에 포개어 두 헤더가 실제 내용 높이를 자동으로 공유하도록 변경했습니다.
- 1366×768 화면에서 고정 헤더와 원본 헤더 높이가 각각 144.275px로 일치하고, 위·아래 경계 차이가 0px인 것을 확인했습니다.
- 페이지를 500px 내린 뒤에도 고정 헤더가 화면 상단에 남고 12~15일 행과 합계가 헤더 아래에 정상적으로 표시되는 것을 확인했습니다.
- 가로 스크롤 후 원본 표와 고정 헤더의 위치 차이가 0px인 것도 재확인했습니다.
- 계획표 테스트 24개, 전체 714개 테스트와 커버리지 기준, 타입 검사, 프로덕션 빌드를 통과했습니다.

## Decisions - 화면 높이에 따른 고정 헤더 겹침과 잘림 수정

글꼴과 화면 배율에 따라 달라지는 헤더 높이를 숫자로 계산하거나 JavaScript로 측정하지 않기로 했습니다. 브라우저의 표 레이아웃이 결정한 실제 높이를 그대로 사용하도록 두 표를 같은 Grid 영역에 겹치는 방식이 화면 크기 변화에 가장 안정적이기 때문입니다.

## Current Status - 화면 높이에 따른 고정 헤더 겹침과 잘림 수정

15인치 노트북에서 흔히 사용하는 1366×768 조건의 월 초반·월 후반 화면을 모두 확인했습니다. 헤더와 본문 겹침, 시작값 잘림, 합계 행 잘림, 가로 위치 불일치는 재현되지 않았고 브라우저 경고나 오류도 없었습니다.

## Next Steps - 화면 높이에 따른 고정 헤더 겹침과 잘림 수정

실제 어머니 노트북의 브라우저 배율이 100%가 아닌 경우에도 같은 화면을 한 번 확인합니다. 구조상 고정 높이를 제거했으므로 다른 배율에서도 자동으로 맞아야 합니다.

## Work Summary - 계획표 이미지 배율과 모바일 미리보기 수정

한 명 이미지의 합계가 잘리고, 인원수에 따라 이미지 배율이 달라지며, 모바일에서 여러 회원 열이 좁은 화면 안에 강제로 축소되던 문제를 수정했습니다. 이미지 높이와 셀 크기를 기준으로 유지하고 회원은 오른쪽으로 이어 붙이는 방식으로 바꿨습니다.

## Work Completed - 계획표 이미지 배율과 모바일 미리보기 수정

- 이미지 생성기가 고정용 헤더 복사본이 아니라 날짜 행과 합계 행이 모두 있는 실제 계획표를 선택하도록 수정했습니다.
- 생성 이미지 상단에 넣던 계획 제목을 제거했습니다. 파일 이름에는 계획 제목이 계속 들어갑니다.
- 원본 계획표의 행 높이를 화면 축소 배율과 분리해 이미지에 적용하여 선택한 회원에 따라 PNG 높이가 달라지지 않게 했습니다.
- 미리보기는 폭에 맞춰 전체 이미지를 줄이지 않고 일정한 높이로 표시하도록 변경했습니다. 회원이 많아 화면 폭을 넘으면 좌우로 움직여 확인할 수 있습니다.
- 가로 스크롤 막대가 나타나는 경우에도 미리보기 높이가 줄지 않도록 스크롤 막대 자리를 항상 확보했습니다.
- 5명 선택 제한과 여섯 번째 회원 비활성화를 제거했습니다.
- 실제 표 선택, 합계 포함, 제목 미포함, 화면 축소 배율을 반영한 행 높이, 6명 선택을 회귀 테스트에 추가했습니다.
- PC 1280×800과 모바일 390×844 화면에서 1명·4명 이미지를 비교하고, 모바일에서 6명 이미지까지 생성했습니다.

## Decisions - 계획표 이미지 배율과 모바일 미리보기 수정

회원 수에 따라 이미지 전체를 화면 폭에 맞추면 글자와 날짜 열의 비율이 계속 달라집니다. 따라서 같은 계획에서는 PNG 높이와 각 셀의 크기를 동일하게 유지하고, 선택한 회원 수만큼 오른쪽 폭만 늘리기로 했습니다. 미리보기 역시 높이를 기준으로 보여 주며, 좁은 화면에서는 가로 스크롤로 나머지 회원을 확인하게 했습니다. 5명 제한은 기술적으로 필요하지 않아 제거했습니다.

## Current Status - 계획표 이미지 배율과 모바일 미리보기 수정

PC에서 1명과 4명 PNG는 모두 높이 1538px로 생성됐고 미리보기 높이도 약 400px로 같았습니다. 모바일에서도 1명과 4명 PNG 높이는 1538px, 미리보기 높이는 약 452px로 일치했습니다. 모바일 4명은 가로 스크롤이 생겼으며, 6명 이미지는 2116×1538px로 정상 생성됐습니다. 이미지 상단에는 `202608A 민경욱` 제목이 들어가지 않고 날짜·회원 열·합계만 표시됩니다. 브라우저 경고와 오류는 없었습니다. 집중 테스트 17개, 전체 715개 테스트와 커버리지 기준, 타입 검사, 프로덕션 빌드, 배포 파일 스모크 검사를 모두 통과했습니다.

## Next Steps - 계획표 이미지 배율과 모바일 미리보기 수정

- 실제 저장된 계획에서 수당 표시와 빨간 표시가 여러 회원 이미지에도 기대한 위치로 출력되는지 최종 사용 확인을 진행합니다.
- 모바일에서 저장한 PNG를 사진 앱으로 열어 확대했을 때 글자 크기와 좌우 이동이 실제 전달 방식에 충분한지 확인합니다.

## Work Summary - 자동 계획의 회원별 1,500/2,500 목표 지원

회원별 보름 좌·우 목표가 모두 2,500이라는 기존 자동 계획 가정과 달리, 실제 운영에서는 회원별로 1,500도 선택할 수 있다는 새 규칙을 자동 계획에 반영했습니다. 수동 계획과 계산 엔진에는 이미 두 목표가 있었지만 자동 계획 입구와 후보 생성은 2,500 전용이어서, 이번 작업에서 혼합 조직까지 같은 계산 경로로 지원하도록 맞췄습니다.

## Work Completed - 자동 계획의 회원별 1,500/2,500 목표 지원

- 설정 화면에서 1,500 회원이 있을 때 `다음 단계`를 막던 안내와 비활성화를 제거했습니다.
- 자동 요청 생성기와 optimizer 요청 검증기에서 1,500을 거부하던 오류를 없애고, 1,500 또는 2,500 이외의 값만 잘못된 요청으로 거부하도록 변경했습니다.
- Constructive 후보 생성기의 하위부터 합산하는 좌·우 총량을 고정 2,500이 아니라 각 회원의 `fortnightSideTarget`으로 계산하도록 변경했습니다.
- 회원별·맨 위 회원 수당 상한도 같은 회원별 목표와 혼합 하위 조직 총량을 사용하도록 변경했습니다.
- 좌·우 목표 1,500과 PVP 목표 2,400이 함께 선택된 경우 목표를 넘는 PVP가 환산 상한을 부풀리지 않도록, matched PV 상한을 실제 좌·우 원본 총량으로 한 번 더 제한했습니다.
- PVP 목표 700 회원의 일반 공평성 평가는 1,500 회원도 구조상 상한으로 비교하되, 화면의 `환산 8회 이상` 인원은 기존 계약대로 좌·우 2,500 회원만 세도록 바로잡았습니다.
- 자동 계획 policy를 `8.0.0`으로 올렸습니다. 문제 fingerprint에 policy와 회원별 좌·우 목표가 포함되므로 policy 7.0의 2,500 전용 후보와 체크포인트는 복원되지 않습니다.
- 1,500 단독 회원, 2,500/1,500 혼합 조직, PVP 2,400과 좌·우 1,500 조합, 잘못된 좌·우 목표, 2,500 전용 8회 표시 조건과 설정 화면 버튼을 회귀 테스트에 추가했습니다.
- 요구사항, 기술 설계, 로드맵, 계산 사례와 Phase 4 실행 계획을 새 계약과 policy 버전에 맞췄습니다.

## Decisions - 자동 계획의 회원별 1,500/2,500 목표 지원

새 기간의 기본 좌·우 목표는 계속 2,500으로 유지하고, 회원별 선택값만 후보 생성과 검증에 적용하기로 했습니다. 좌·우 1,500을 단순히 고정 `환산 5회` 하드 조건으로 만들지 않았습니다. 하위 조직이 없는 최소 총량 회원은 이론상 5회지만 상위 회원은 하위 실적에 따라 더 높을 수 있으므로, 기존 목적함수처럼 회원별 구조상 상한과 실제 환산값의 미달을 비교하는 것이 맞기 때문입니다. 목적 순서와 점수 의미는 바뀌지 않아 objective는 `7.0.0`을 유지했고, 지원 문제와 Constructive 총량이 바뀌었으므로 policy만 `8.0.0`으로 올렸습니다.

## Current Status - 자동 계획의 회원별 1,500/2,500 목표 지원

좌·우 1,500·PVP 700 단독 회원은 최소 총 신규 PV 3,000, 좌·우 1,500·PVP 2,400 단독 회원은 3,900으로 검증됐습니다. 좌·우 2,500 상위 회원 아래 좌·우 1,500 하위 회원이 있는 혼합 조직은 최소 총 신규 PV 5,500으로 두 회원 모두 `allTargetsMet=true`를 확인했습니다. 전체 60개 테스트 파일의 721개 테스트, TypeScript 검사, 프로덕션 빌드와 배포 파일 스모크 검사를 통과했습니다. 번들 크기 500kB 경고는 기존과 같이 남아 있지만 빌드 실패나 이번 규칙 변경 문제는 아닙니다. 이번 변경은 로컬 작업이며 아직 커밋·푸시·배포하지 않았습니다.

## Next Steps - 자동 계획의 회원별 1,500/2,500 목표 지원

- `202608A 민경욱 · 자동 계산 비교` 사본에서 policy 8.0 자동 계산을 실행해 수동 최종본과 회원별 환산 횟수·수당·총 구매량·날짜 배치를 비교합니다.
- 이번 작업은 1,500 규칙을 올바르게 계산할 기반을 만든 범위입니다. 수동 최종본을 정답 fixture로 추출하고 조직 단위 날짜 이동 후보를 개선하는 Phase 4 휴리스틱 작업은 비교 결과를 고정한 뒤 별도로 진행합니다.

## Work Summary - 202608A 수동 정답지 기반 Phase 4 휴리스틱 개선

엄마가 직접 완성하고 만족한다고 확인한 `202608A 민경욱` 수동 계획을 승인 정답지 회귀로 고정했습니다. 기존 자동안이 유효한 계획은 빠르게 만들지만 좌·우 하위 조직의 실적을 같은 날짜 수당 경계에 맞추지 못하던 문제를 해결하기 위해, 점수 조정이 아니라 실제 후보 생성 범위를 조직 단위로 확장했습니다. 동시에 100PV 억지 구매를 막는 절대 상한과 새 목적 순서를 계약으로 확정했습니다.

## Work Completed - 202608A 수동 정답지 기반 Phase 4 휴리스틱 개선

- 클라우드·계정·프로젝트 UUID와 실제 회원 ID를 제거한 `202608A` fixture에 엄마 수동안과 policy 8 자동 비교안을 함께 고정했습니다.
- 엄마 수동안은 총 신규 PV 23,510, 확인 수당 6,000,000원, 맨 위 회원 목표 미달 0일, 소멸 34,355로 같은 Phase 1 평가기를 통과하도록 고정했습니다.
- policy 8 비교안은 총 신규 PV 23,500, 확인 수당 5,640,000원, 맨 위 회원 목표 미달 0일, 소멸 38,315로 고정했습니다.
- 목표 700을 포함한 비루트 회원의 하위 좌·우 가지를 함께 날짜 profile로 재배치하고, 가지 전체 회전과 총량 보존 날짜 이동을 후보에 추가했습니다.
- 현재 날짜의 다음 300·700·1,500·2,400 수당 경계 부족량을 계산하고, 다른 날짜의 현재 수당 단계를 해치지 않는 좌·우 하위 가지 실적을 동시에 옮기는 조직 경계 후보를 추가했습니다.
- 전역 점수는 잠시 나빠져도 특정 회원의 환산 횟수가 좋아진 후보를 bounded beam에 남겨, 여러 셀을 함께 바꿔야 넘을 수 있는 local valley를 통과하도록 했습니다.
- 본인 `SELF` 실적과 PVP를 같은 총구매량 안에서 교환하고, 기존 PVP headroom과 0 또는 30 이상 직접값 규칙을 지키는 후보를 추가했습니다.
- 구조상 최소 총 신규 PV를 `M`으로 독립 계산하고 모든 후보를 `M+30` 이하로 제한했습니다. +31PV 또는 +100PV 후보는 수당이 늘어도 verifier가 거부합니다.
- `M..M+30` 안에서 기존 30 이상 셀에 1~30PV를 보태거나 새 30PV 셀을 만드는 마무리 후보를 추가했습니다.
- 목적 순서를 `맨 위 회원 목표 미달 최소화 → 확인 수당 최대화 → 총 신규 PV 최소화 → 기존 후순위 목적`으로 변경했습니다.
- policy를 `9.0.0`, objective를 `8.0.0`으로 올렸습니다. 문제 fingerprint와 checkpoint의 현재 objective·engine 검사로 구버전 저장 후보가 새 결과처럼 복원되지 않는 회귀 테스트를 추가했습니다.
- 동일 입력·버전·고정 seed에서 새 휴리스틱이 총 신규 PV 23,500, 확인 수당 6,060,000원, 맨 위 회원 목표 미달 0일, 소멸 32,952를 만들고 전체 allocation이 고정 fingerprint `49db36d5f058dd4b`와 일치하도록 고정했습니다.
- 새 결과의 주요 환산 횟수는 베로니카 10, 고규식 5, 김정미 7, 까리나 김 5, Kelly 18, 남승우 8, 김길주 8, 시아원 8, 박진숙 8입니다.
- 57명 조직에서 목표 700 전 회원의 초기 profile을 전부 펼치며 전체 테스트가 7분 이상 끝나지 않던 문제를 확인했습니다. 20명 이하는 전 회원을 유지하고, 20명 초과 조직은 초기 profile을 PVP 목표 내림차순·정본 순서 12명으로 제한한 뒤 현재 환산 미달이 큰 최대 12명을 다시 고르도록 했습니다. 대규모 성능 테스트 두 개는 수정 후 약 3.7초에 끝났습니다.
- 현재 정답지가 아닌 202407 자료에서 전체 worker를 중복 실행하던 장시간 기대값 테스트는 제거하고, 총량 보존 profile·가지 이동 단위 회귀만 유지했습니다.
- 영업일의 `assessedLeft/Right`는 null일 수 없고 null은 `SKIPPED` 날짜뿐이라는 엔진 계약을 확인해, 조직 경계 생성기의 실행 불가능한 fallback 분기를 제거했습니다.
- 요구사항, 기술 설계, 로드맵, 계산 사례와 Phase 4 실행 계획을 policy 9·objective 8·`M+30`·대규모 후보 예산 계약에 맞췄습니다.

## Decisions - 202608A 수동 정답지 기반 Phase 4 휴리스틱 개선

엄마의 날짜별 셀을 정답으로 복제하지 않고, 현재 PVP 작은 쪽 합산 규칙으로 같은 계획을 다시 평가한 결과와 전체 allocation fingerprint를 회귀선으로 사용했습니다. 수동안은 알고리즘이 추구할 배치 목적을 보여 주지만 과거 규칙이나 사람별 선택까지 전역 최적해라고 증명하지는 않기 때문입니다.

100PV 추가 구매 금지는 낮은 우선순위 점수로 두지 않고 `M+30` 독립 hard ceiling으로 만들었습니다. 그 안에서는 맨 위 회원의 기간 활동 목표를 먼저 지키고 실제 수당을 최대화한 뒤, 같은 결과끼리만 지출을 줄입니다. 이 순서로 비용 폭주를 막으면서도 실제로 필요한 1~30PV 끝자리 마무리는 허용합니다.

새 결과가 엄마 수동안보다 총수당은 60,000원 높고 신규 PV는 10 적지만, Kelly·김정미·박진숙의 개인 환산은 각각 엄마안보다 1 낮습니다. 따라서 “엄마 계획을 복제했다”거나 “전역 최적해를 찾았다”고 표현하지 않고, 총수당 회귀와 개인별 공평성 회귀를 분리해 유지합니다.

대규모 조직 상한은 회원을 평가에서 제외하는 규칙이 아니라 브라우저 후보 생성 예산입니다. 전 회원의 목적값은 계속 계산하며, 20명 초과에서는 초기 12명과 현재 미달 focus 12명으로 후보 폭증을 제어합니다.

## Current Status - 202608A 수동 정답지 기반 Phase 4 휴리스틱 개선

- `202608A` 정답지 회귀는 약 78초에 통과했고, 고정 수당·PV·소멸·회원별 환산·전체 allocation fingerprint가 모두 일치했습니다.
- 기본 전체 테스트는 62개 파일, 728개 테스트가 모두 통과했습니다.
- 전체 커버리지는 statements 95.60%, branches 91.75%, functions 96.81%, lines 96.25%이며, optimizer branches 95.51%를 포함해 모든 경로별 설정 하한을 통과했습니다.
- TypeScript 검사, 프로덕션 빌드와 배포 파일 스모크 검사를 통과했습니다. 기존 500kB 초과 번들 경고는 남아 있지만 이번 변경의 실패는 아닙니다.
- 이 작업은 로컬 변경이며 아직 커밋·푸시·배포하지 않았습니다.

## Next Steps - 202608A 수동 정답지 기반 Phase 4 휴리스틱 개선

- `202608A 민경욱 · 자동 계산 알고리즘 테스트용` 계획에서 새 policy 9 결과를 실제 브라우저로 한 번 실행해, 저장된 구버전 후보가 복원되지 않고 새 계산이 표시되는지 확인합니다.
- 엄마 수동안과 새 자동안의 날짜별 차이를 화면에서 비교해, 총수당 +60,000원과 Kelly·김정미·박진숙 개인 환산 -1의 교환을 실제 운영에서 받아들일지 확인합니다.
- 21명 이상과 57명 실제 조직에서는 초기 12명 상한을 포함한 worker의 첫 verified candidate 시간과 메모리를 대상 노트북에서 별도 측정합니다.
- 개인별 공평성을 더 개선할 때는 총수당·맨 위 회원 목표·`M+30`을 악화시키지 않는 별도 후보 확장으로 진행합니다.

## Work Summary - 모바일 계획표 이미지 고정 출력

아이폰에서 생성한 PNG가 날짜 열만 화면 전체 폭으로 늘어나고 회원 열은 사라지던 문제를 수정했습니다. 현재 브라우저에 보이는 HTML 표를 그대로 캡처하지 않고, PC와 모바일에서 동일한 규격의 계획표 이미지를 직접 그리도록 생성 방식을 교체했습니다.

## Work Completed - 모바일 계획표 이미지 고정 출력

- 브라우저 CSS와 SVG의 HTML 삽입 기능에 의존하던 이미지 생성을 제거했습니다.
- 날짜 열 50px, 회원 한 명당 `PVP·좌·우` 합계 168px, 고정 행 높이를 사용하는 순수 SVG 출력기를 추가했습니다.
- 회원 수가 늘어나면 높이와 셀 크기는 유지하고 오른쪽으로 회원 열만 추가되도록 했습니다.
- 날짜, 회원 이름과 ID, 목표값, 잔액, 시작값, 날짜별 입력값, 합계가 고정 규격으로 출력되도록 했습니다.
- 수당 발생 시 왼쪽의 얇은 색 표시와 실제 차이의 빨간 가로 테두리도 새 출력기에 유지했습니다.
- 계획 제목은 이미지 안에 넣지 않고 기존처럼 파일 이름에만 사용합니다.
- 순수 SVG에 회원 값·합계·수당 표시·빨간 표시가 포함되고 `foreignObject`가 포함되지 않는 회귀 테스트를 추가했습니다.
- PC 1280×800과 모바일 390×844에서 같은 선택을 각각 생성해 실제 PNG 크기와 회원 열 표시를 확인했습니다.

## Decisions - 모바일 계획표 이미지 고정 출력

기존 방식은 HTML 표와 현재 화면의 계산된 CSS를 SVG 안에 복사한 뒤 캔버스로 변환했습니다. iPhone Safari는 이 과정에서 복제된 표의 열 너비를 데스크톱과 다르게 해석해 날짜 열만 남기는 문제가 있었습니다. 따라서 화면을 캡처하거나 브라우저 레이아웃을 재사용하지 않고, 이미지 전용 좌표와 색으로 표를 직접 그리는 방식을 선택했습니다. 이 방식이면 휴대전화 화면 폭이나 미리보기 크기가 PNG의 실제 구조에 영향을 주지 않습니다.

## Current Status - 모바일 계획표 이미지 고정 출력

PC와 모바일 모두 1명 결과가 436×1538px, 4명 결과가 1444×1538px로 동일하게 생성됐습니다. 모바일에서 만든 1명 결과에도 날짜 열과 민경욱의 `PVP·좌·우` 열, 마지막 합계가 함께 들어갔습니다. 4명 결과도 높이는 유지되고 오른쪽으로 회원 열만 늘어났으며 브라우저 경고와 오류는 없었습니다.

## Next Steps - 모바일 계획표 이미지 고정 출력

- 실제 아이폰 Safari에서 이미지를 한 번 저장해 사진 앱에 날짜와 선택한 회원 열이 모두 보이는지 최종 확인합니다.
- 실제 저장 계획에 수당 표시와 빨간 표시가 있는 경우 새 출력 이미지에서도 색 위치가 기대한 셀과 일치하는지 확인합니다.

## Work Summary - 엄마 피드백 기반 완성형 수당 경계 이동

엄마가 표시한 김정미·민경욱·Kelly 사례를 일반 규칙으로 분석했습니다. 한 날짜의 다음 수당 경계에 300PV가 필요할 때 기존 생성기는 100 또는 200만 옮긴 미완성 후보를 먼저 채워, 서로 다른 하위 칸에서 `200+100`을 모으면 경계를 완성할 수 있는 후보를 놓치고 있었습니다. 완성형 다중 이동을 후보에 남기고, 총수당과 총구매량이 같을 때 핵심 회원의 환산 공평성을 소멸량보다 먼저 지키도록 목적 순서를 조정했습니다.

## Work Completed - 엄마 피드백 기반 완성형 수당 경계 이동

- 조직 경계 이동 후보가 계산된 부족량을 실제로 전부 채우지 못하면 후보 목록에 들어가지 않도록 했습니다.
- 큰 여유, PVP보다 좌·우 실적 우선, 날짜 순서뿐 아니라 작은 칸 우선, 100에 가까운 칸 우선, 부족량과 가까운 여유 우선 순회를 추가해 `200+100` 같은 조합을 결정적으로 생성합니다.
- 서로 다른 하위 직접 입력 칸에서 200PV와 100PV를 옮겨 같은 날짜의 300 단계 경계를 완성하고 총 직접 구매량을 유지하는 최소 회귀 테스트를 추가했습니다.
- 목적 순서를 `맨 위 회원 목표 미달 → 확인 수당 → 총 신규 PV → 목표 1,500·2,400 공평성 → 목표 700 공평성 → 초과 소멸량`으로 변경했습니다.
- policy를 `10.0.0`, objective를 `9.0.0`으로 올려 이전 자동 후보와 브라우저 작업 체크포인트가 새 계산처럼 복원되지 않게 했습니다.
- `202608A` 회귀에서 전 회원의 기간 목표 충족을 검사하고, 김정미 회원이 `신규 PVP 200 + 원본 좌 2,300 = 평가 좌 2,500`으로 목표를 채우는 사실을 별도로 고정했습니다.
- 새 결과는 총 신규 PV 23,500, 확인 수당 6,060,000원, 소멸 32,756이며 주요 환산 횟수는 기존 policy 9의 공평한 분포인 베로니카 10, 고규식 5, 김정미 7, 까리나 김 5, Kelly 18, 남승우 8, 김길주 8, 시아원 8, 박진숙 8을 유지합니다.
- 결과 전체 allocation fingerprint를 `dcf9dc632d750079`로 갱신해 동일 입력·버전에서 결정적 결과를 보장했습니다.
- 요구사항, 로드맵, 기술 설계, 계산 사례와 Phase 4 실행 계획을 policy 10·objective 9 계약에 맞췄습니다.

## Decisions - 엄마 피드백 기반 완성형 수당 경계 이동

엄마가 적은 이름과 특정 날짜를 하드코딩하지 않고, `다음 경계 부족량을 여러 하위 칸의 총량 보존 이동으로 완성한다`는 일반 후보 규칙으로 구현했습니다. 김정미의 `250+2,150=2,400` 백업 상태는 현재 policy 9 정본 재계산 결과와 달랐습니다. 현재 정본 후보는 이미 `200+2,300=2,500`으로 목표를 통과하므로, 백업 화면의 상태를 새 계산에서도 재현했다고 주장하지 않고 구버전 또는 적용된 별도 계획과 새 자동 후보를 구분합니다.

후보 조합 상한을 5개에서 12개로 늘리는 실험도 했습니다. 총수당은 6,060,000원으로 같았지만 Kelly 22회 대신 남승우 5회·김길주 7회처럼 핵심 회원 분포가 크게 흔들렸습니다. 후보 폭만 늘리는 방식은 채택하지 않고, 상한 5개 안에 경계를 완성하는 서로 다른 결정적 순서를 넣었습니다.

같은 맨 위 목표·수당·총구매량이라면 엄마가 실제로 신경 쓰는 핵심 회원 환산 공평성을 일일 소멸량보다 먼저 비교합니다. 다만 총수당 최대화와 불필요한 직접 구매 최소화는 계속 그보다 앞선 목적입니다.

## Current Status - 엄마 피드백 기반 완성형 수당 경계 이동

- 집중 회귀 31개와 전체 62개 테스트 파일의 729개 테스트가 모두 통과했습니다.
- 전체 커버리지는 statements 95.44%, branches 91.58%, functions 96.85%, lines 96.23%입니다.
- TypeScript 검사, 프로덕션 빌드와 배포 파일 스모크 검사를 통과했습니다. 650.91kB 번들 경고는 기존 빌드 경고이며 이번 변경 실패는 아닙니다.
- 변경은 로컬에만 있으며 아직 커밋·푸시·배포하지 않았습니다.

## Next Steps - 엄마 피드백 기반 완성형 수당 경계 이동

- 변경을 커밋·푸시·배포한 뒤 J님이 만든 백업 계획에서 자동 계산을 새로 실행해 policy 10 결과인지 확인합니다.
- 백업에 남은 엄마 수동안은 건드리지 않고, 자동 계산용 사본에서 김정미 기간 목표와 Kelly·김정미·박진숙 환산 횟수를 비교합니다.
- 새 자동안이 엄마가 지적한 날짜에서 어떤 하위 칸을 묶어 옮겼는지 보여 드리고, 총수당이 같아도 실제 운영에서 더 선호하는 개인별 배분이 있는지 추가 의견을 받습니다.
