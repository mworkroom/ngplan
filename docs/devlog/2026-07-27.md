# 2026-07-27 ngplan Development Log

## Work Summary - 플랜 진입 UX 단순화

입력 완료 뒤 별도의 준비 완료 카드에서 버튼을 다시 찾아야 했던 흐름을 없애고, 설정 화면 상단에서 수동 플랜 또는 자동 플랜으로 바로 이동할 수 있도록 정리했다. 같은 의미의 상태 문구가 여러 번 반복되어 화면의 핵심 행동이 흐려지는 문제도 함께 해소했다.

## Work Completed - 플랜 진입 UX 단순화

- 상단 동작을 `수동 플랜 열기`와 `자동 플랜 만들기`로 명확히 구분했다.
- 두 버튼을 누르는 순간 입력값을 검증하고, 유효할 때 선택한 화면으로 바로 이동하도록 했다.
- `계획표 준비 완료`, `플랜을 만들 준비가 되었습니다`, `준비 완료` 문구와 준비 완료 카드를 제거했다.
- 다중 사용자 작업 표시를 위해 남아 있던 `입력 중` 상태를 제거했다.
- 화면 전환 뒤 새 화면 제목으로 초점이 이동하도록 하고 키보드 Enter 동작을 회귀 테스트에 포함했다.
- 모바일 너비에서 상단 버튼이 한 열로 배치되고 화면 밖으로 넘치지 않도록 반응형 스타일을 보완했다.

## Decisions - 플랜 진입 UX 단순화

- 수동 플랜과 자동 플랜은 서로 다른 최종 행동이므로, 중간 `플래너 생성` 단계로 묶지 않고 처음부터 각각의 행동명으로 노출하기로 했다.
- 준비 상태를 반복해서 알리는 것보다 사용자가 다음 행동을 즉시 선택할 수 있게 하는 편이 인지 부담이 적다고 판단했다.
- 화면 전환은 시각적 변화만으로 알리지 않고 새 화면 제목으로 초점을 옮겨 키보드 및 보조기기 사용자도 전환을 인지할 수 있게 했다.

## Current Status - 플랜 진입 UX 단순화

- 수동·자동 플랜 직접 진입, 입력 검증, 키보드 실행, 화면 제목 초점 이동을 자동 테스트로 확인했다.
- 데스크톱 및 모바일 너비의 버튼 배치를 로컬 시각 점검으로 확인했다.
- 로그인된 실제 운영 화면에서의 최종 확인은 배포 후 별도로 필요하다.

## Next Steps - 플랜 진입 UX 단순화

- 실제 배포 환경에서 로그인 후 Tab과 Enter만으로 입력부터 화면 전환까지 완료되는지 최종 확인한다.

## Work Summary - 계획 목록 이중 시간 표시

계획 목록의 저장 시각이 브라질 시간만 표시되어 한국에서 최근 저장 시점을 바로 파악하기 어려운 문제를 해결했다. 하나의 저장 시각을 한국과 브라질 시간으로 각각 변환해 두 줄로 표시한다.

## Work Completed - 계획 목록 이중 시간 표시

- 계획 카드에 `한국 시간`을 첫 줄, `브라질 시간`을 둘째 줄로 표시하도록 변경했다.
- 한국은 `Asia/Seoul`, 브라질은 기존 기준인 `America/Sao_Paulo` 시간대를 명시적으로 사용했다.
- 동일한 UTC 저장 시각이 두 시간대에서 정확한 날짜와 오전·오후 시각으로 변환되는지 테스트를 추가했다.
- 현재 UI 문구와 달라진 기존 테스트의 필드명 및 `불러오기` 버튼명을 실제 접근성 이름에 맞게 정리했다.

## Decisions - 계획 목록 이중 시간 표시

- 서버 저장값 자체는 변경하지 않고 화면에서만 두 시간대로 변환한다. 이 방식은 하나의 정확한 저장 시점을 유지하면서도 한국과 브라질 양쪽에서 쉽게 확인할 수 있다.
- 한국 사용자가 먼저 확인할 정보를 우선 배치해 `한국 시간`을 위에 표시한다.

## Current Status - 계획 목록 이중 시간 표시

- 예시 저장값은 `한국 시간 2026. 7. 27. 오전 1:58`, `브라질 시간 2026. 7. 26. 오후 1:58`로 표시됨을 확인했다.
- 전체 자동 테스트 646개, TypeScript 타입 검사, 프로덕션 빌드가 모두 통과했다.
- 빌드 시 기존의 단일 JavaScript 청크 크기 경고는 남아 있으나 빌드는 정상 완료됐다.

## Next Steps - 계획 목록 이중 시간 표시

- 배포 후 실제 계획 목록에서 두 줄의 시간 표시와 모바일 줄바꿈을 최종 확인한다.

## Work Summary - 미입력 경고 집중화

조직 구조의 미입력 안내와 이동 버튼이 양 끝으로 떨어져 있어 버튼만 먼저 보이고 문제의 맥락을 놓치기 쉬운 문제를 개선했다. 회원 입력창 위에 반복되던 현재 회원 경고도 제거해 사용자가 확인해야 할 경고 위치를 한곳으로 줄였다.

## Work Completed - 미입력 경고 집중화

- 조직 구조 경고 막대의 안내 문구와 `첫 번째 문제 보기` 버튼을 오른쪽에 모으고 간격을 6px로 줄였다.
- 좁은 화면에서는 두 요소가 함께 자연스럽게 줄바꿈되도록 했다.
- 회원 입력창 위의 `현재 회원: 수정할 항목 N개` 경고와 이동 버튼을 제거했다.
- 기존 중복 경고가 담당하던 오류 및 보관함 항목 초점 이동을 조직 구조의 단일 이동 버튼이 계속 수행하도록 회귀 테스트를 수정했다.
- 현재 화면 문구와 달라져 있던 클라우드 화면의 목록 이동 버튼 테스트를 실제 접근성 이름에 맞췄다.

## Decisions - 미입력 경고 집중화

- 70대 초보 사용자가 버튼만 따로 읽어도 맥락을 잃지 않도록 경고 내용과 해결 행동을 하나의 시각적 묶음으로 유지하기로 했다.
- 같은 오류 개수를 회원 입력 영역에서 다시 보여주는 것은 발견성을 높이기보다 경고 위치를 분산시키므로 제거했다.
- 오류 이동 기능은 없애지 않고 조직 구조 경고의 `첫 번째 문제 보기` 버튼으로 통합했다.

## Current Status - 미입력 경고 집중화

- 1948px 화면에서 안내와 버튼의 실제 간격이 약 6px임을 확인했다.
- 500px 화면에서 두 요소가 가까이 유지되고 문서 가로 너비가 화면을 넘지 않음을 확인했다.
- 회원 입력 영역의 중복 경고가 렌더링되지 않는 것을 확인했다.
- 전체 자동 테스트 646개, TypeScript 타입 검사, 프로덕션 빌드가 모두 통과했다.
- 운영 화면은 로그인으로 보호되어 있어 동일한 앱 컴포넌트를 임시 로컬 검증 화면에 렌더링해 확인했으며, 검증용 파일은 작업 후 삭제했다.

## Next Steps - 미입력 경고 집중화

- 배포 후 실제 저장된 계획에서 경고 버튼을 눌렀을 때 첫 미입력 필드로 초점과 화면 위치가 자연스럽게 이동하는지 최종 확인한다.

## Work Summary - 보관함 회원 다시 넣기 UX

회원을 조직도에서 잠시 빼 둔 상태를 개발자 관점의 `새 위치 결정 대기`가 아니라 사용자가 이해하기 쉬운 `보관함`으로 표현했다. 다시 넣는 방법과 정보 화면으로 돌아가는 방법도 화면에서 바로 알 수 있도록 흐름을 정리했다.

## Work Completed - 보관함 회원 다시 넣기 UX

- 대기 회원 영역의 제목을 `보관함에 있는 회원`으로 바꾸고, 원하는 빈 자리의 `+` 버튼을 누르라는 다음 행동을 안내했다.
- 보관함의 회원 항목 전체를 큰 `정보 보기` 버튼으로 바꾸어 다른 회원을 선택한 뒤에도 해당 회원의 우측 정보 화면으로 돌아올 수 있게 했다.
- 선택 중인 보관함 회원은 `aria-pressed` 상태와 배경색으로 구분되도록 했다.
- 우측 회원 정보 영역의 안내를 빈 자리의 `+` 버튼과 `이 자리에 넣기` 버튼을 순서대로 설명하는 문구로 교체했다.
- 빈 자리 선택창의 제목을 `누가 들어갈까요?`로 바꾸고, `새 회원 만들기`와 보관함 회원을 넣는 행동을 쉬운 문장으로 설명했다.
- 보관함에 넣은 직후의 알림과 검증 오류 문구도 같은 용어와 행동 순서를 사용하도록 통일했다.
- 보관함 회원 재선택과 키보드 Enter 선택을 자동 테스트에 추가했다.

## Decisions - 보관함 회원 다시 넣기 UX

- `새 위치를 정해 주세요`처럼 보관함 카드 자체에서 이동을 시작하는 것으로 오해할 수 있는 표현은 제거했다.
- 사용자가 기억해야 할 절차를 줄이기 위해 모든 안내에서 `조직도의 원하는 빈 자리에서 + 버튼 누르기`를 첫 행동으로 반복했다.
- 별도의 작은 `회원 정보 보기` 버튼보다 회원 항목 전체를 선택 버튼으로 사용해 클릭 영역을 넓히고 키보드 접근성을 함께 확보했다.

## Current Status - 보관함 회원 다시 넣기 UX

- 로컬 브라우저에서 회원 생성, 보관함 이동, 다른 회원 선택, 보관함 회원 정보로 복귀, 빈 자리 연결창 표시까지 실제 흐름을 확인했다.
- 전체 자동 테스트 646개, TypeScript 타입 검사, 프로덕션 빌드가 모두 통과했다.
- 빌드는 정상 완료됐으며 기존 JavaScript 청크 크기 경고는 남아 있다.
- 검증용 임시 화면은 확인 후 삭제했다.

## Next Steps - 보관함 회원 다시 넣기 UX

- 배포 후 실제 저장된 계획에서 보관함 회원을 다시 연결했을 때 회원과 하위 조직이 예상한 위치로 함께 이동하는지 최종 확인한다.

## Work Summary - 회원 종류 상태표시 제거

회원 정보 입력 제목 옆의 `최상위 회원`과 `등록된 회원` 표시는 사용자가 별도로 판단하거나 조작할 정보가 아니며 조직도에서 이미 알 수 있어 화면에서 제거했다.

## Work Completed - 회원 종류 상태표시 제거

- 회원 정보 입력 패널의 회원 종류 상태 배지를 제거했다.
- 최상위 회원과 일반 회원 양쪽에서 해당 문구가 표시되지 않는 회귀 테스트를 추가했다.

## Decisions - 회원 종류 상태표시 제거

- 회원의 최상위 여부는 이동·삭제 로직에 필요하므로 내부 상태와 조건 분기는 유지했다.
- 보관함 안내처럼 다음 행동을 설명하는 정보는 유지하고, 단순히 내부 분류를 반복하는 문구만 제거했다.

## Current Status - 회원 종류 상태표시 제거

- 관련 컴포넌트 테스트 12개와 TypeScript 타입 검사가 통과했다.
- 프로덕션 빌드가 통과했으며 기존 JavaScript 청크 크기 경고는 남아 있다.

## Next Steps - 회원 종류 상태표시 제거

- 배포 후 회원 정보 제목 영역의 여백이 실제 모바일 화면에서도 자연스러운지 확인한다.

## Work Summary - 수동·자동 계획 화면 분리

수동 계획표에서 자동 계산으로 전환하는 드문 상황을 전제로 자동 계획 패널을 계속 노출하던 구조를 실제 업무 흐름에 맞게 분리했다. 화면 종류를 버튼 유무로 추측하지 않도록 각 계획표의 제목도 명시적으로 구분했다.

## Work Completed - 수동·자동 계획 화면 분리

- 수동 플랜으로 들어오면 `수동 계획표`만 표시하고 자동 계획 패널과 관련 버튼은 렌더링하지 않도록 했다.
- 자동 플랜으로 들어오면 화면 상태를 별도로 저장하고 `자동 계획 만들기`와 `자동 계획표`를 표시하도록 했다.
- 자동 계산의 진행·완료·중지·실패 상태 문구에서 `검증 계획`, `최소값`, `최적성 증명` 같은 개발자 표현과 중복 안내를 제거했다.
- `검증 계획 확인·적용`을 `결과 확인하기`로, 최종 적용 버튼을 `이 결과를 계획표에 넣기`와 `계획표에 넣기`로 바꿨다.
- 자동 결과 확인 제목과 재계산·중지 버튼도 행동이 바로 드러나는 문구로 정리했다.
- 수동 화면에 자동 계획 UI가 없는지, 자동 화면에 자동 계획 제목과 계획표가 있는지, 저장 후에도 화면 종류가 유지되는지 회귀 테스트를 보강했다.

## Decisions - 수동·자동 계획 화면 분리

- `계획표`라는 공통 제목과 버튼 유무만으로 화면 종류를 추측하게 하지 않고 `수동 계획표`와 `자동 계획표`를 각각 사용한다.
- 자동 계산 결과를 계획표에 넣을 때 기존 직접 입력값이 바뀔 수 있으므로 결과 확인과 최종 확인 단계는 유지하되 쉬운 문구를 사용한다.
- 기간 중 이미 사용한 날짜를 고정하고 남은 기간만 재계산하는 기능은 이후 별도 수정 흐름에서 제공하며 수동 계획표의 상시 기능으로 두지 않는다.

## Current Status - 수동·자동 계획 화면 분리

- 관련 화면·자동 계산·저장 회귀 테스트 26개와 TypeScript 타입 검사, 프로덕션 빌드가 통과했다.
- 전체 테스트에서는 646개 중 642개가 통과했다. 나머지 4개는 이번 작업 전부터 수정 중이던 조직도 경고 버튼명과 회원 검색 표시 이름 문구가 기존 테스트 기대값과 달라 발생했으며 이번 변경 범위 파일은 아니다.
- 빌드는 정상 완료됐으며 기존 JavaScript 청크 크기 경고는 남아 있다.

## Next Steps - 수동·자동 계획 화면 분리

- 자동 계산 알고리즘이 준비되면 실제 브라우저에서 계산 시작, 결과 확인, 계획표 반영까지의 전체 흐름을 다시 확인한다.
- 이후 기간 중 수정 기능에서는 지난 날짜를 고정한 뒤 남은 날짜만 자동 재계산하거나 직접 수정하는 진입점을 별도로 설계한다.

## Work Summary - 상세 계산 결과 기본 접기

계획표 아래의 일별 계산 과정과 보름·전체 회원 요약은 계산 검증에는 유용하지만 일상적으로 표를 작성하는 주사용자에게는 정보가 과도했다. 미래의 셀별 계산 근거 기능에 재사용할 수 있도록 삭제하지 않고 기본적으로 접어 두었다.

## Work Completed - 상세 계산 결과 기본 접기

- `오늘 계산 결과`, `보름 목표 요약`, `전체 회원 마감 현황`을 `상세 계산과 전체 현황 보기` 영역 안으로 묶었다.
- 상세 영역은 화면을 열었을 때 닫힌 상태이며 필요한 경우에만 펼칠 수 있도록 했다.
- 숫자의 계산 과정이나 전체 보름 결과를 확인할 때 사용하는 영역이라는 짧은 안내를 추가했다.
- 키보드로 동작하는 기본 `details`와 `summary` 요소를 사용하고 펼침 상태 회귀 테스트를 추가했다.

## Decisions - 상세 계산 결과 기본 접기

- 일별 계산 결과 데이터는 전날 이월, 오늘 실적, PVP 적용, 커미션, 다음 날 이월을 이미 구조화하고 있어 향후 셀별 `계산 근거 보기`와 `설명 복사` 기능의 기반으로 유지한다.
- 우클릭은 모바일에서 사용할 수 없고 초보 사용자가 발견하기 어려우므로 미래 기능의 주 경로로 삼지 않는다. 셀 선택 뒤 보이는 명시적인 버튼을 기본 경로로 검토한다.
- 보름·전체 회원 요약은 셀 하나의 산출 근거와는 역할이 다르지만 관리자 검토에 사용할 수 있어 함께 접어 보존한다.

## Current Status - 상세 계산 결과 기본 접기

- 관련 수동·자동 계획 테스트 30개와 TypeScript 타입 검사, 프로덕션 빌드가 통과했다.
- 빌드는 정상 완료됐으며 기존 JavaScript 청크 크기 경고는 남아 있다.

## Next Steps - 상세 계산 결과 기본 접기

- 향후 셀별 계산 근거 기능에서는 선택한 셀의 입력값과 계산 단계를 짧은 문장으로 만들고 `설명 복사` 기능으로 ChatGPT에 전달할 수 있게 한다.
- 실제 사용자 확인 후 보름·전체 회원 요약이 전혀 쓰이지 않는다면 별도 관리자 화면으로 이동하거나 UI에서 제거한다.

## Work Summary - 상세 조회 선택 도구 이동

계획표 위쪽의 날짜·회원 이동 메뉴와 표 아래의 현재 선택 문장이 같은 정보를 반복하고 있었다. 상세 계산을 볼 때만 필요한 조회 도구를 접힌 상세 영역 안으로 옮겨 계획표 본문을 단순화했다.

## Work Completed - 상세 조회 선택 도구 이동

- 계획표 상단의 `날짜 결과 보기`와 `회원으로 이동` 드롭다운을 제거했다.
- 상세 토글 안에 `날짜 선택`과 `회원 선택` 드롭다운을 배치했다.
- 토글 위에 별도로 표시되던 `선택: 날짜 · 회원` 문장을 제거했다.
- 상세 조회 드롭다운을 바꿔도 계획표 입력칸으로 초점과 화면이 강제로 이동하지 않고 상세 계산 내용만 바뀌도록 했다.
- 선택한 날짜·회원에 연결된 상세 오류 안내도 토글 안으로 이동했다.

## Decisions - 상세 조회 선택 도구 이동

- 날짜·회원 드롭다운은 계획표 이동 기능이 아니라 상세 계산의 조회 조건으로 정의했다.
- 현재 조회 대상은 두 드롭다운 자체에서 확인할 수 있으므로 별도의 선택 요약 문장을 반복하지 않는다.

## Current Status - 상세 조회 선택 도구 이동

- 관련 수동·자동 계획 테스트 46개와 TypeScript 타입 검사, 프로덕션 빌드가 통과했다.
- 빌드는 정상 완료됐으며 기존 JavaScript 청크 크기 경고는 남아 있다.

## Next Steps - 상세 조회 선택 도구 이동

- 향후 셀별 `계산 근거 보기`를 구현할 때는 표에서 선택한 셀의 날짜와 회원을 상세 조회 드롭다운에 자동 반영할지 검토한다.
