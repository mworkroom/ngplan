# 2026-07-28 ngplan Project Development Log

## Work Summary

루트에 놓인 favicon 파일 때문에 기존 정적 자산 경로가 비어 있던 문제를 복구하고, 계획표 앱에서 favicon이 제공되도록 확인했다.

## Work Completed

- favicon SVG를 `public/assets/favicon.svg`에 복구했다.
- 기존 `index.html`의 `/assets/favicon.svg` 링크와 실제 파일 경로를 일치시켰다.
- 루트에 중복으로 놓인 favicon 파일은 제거했다.
- 프로덕션 빌드와 dist smoke check에서 favicon 자산 포함을 확인했다.

## Decisions

- Vite 정적 자산은 `public` 아래에 보관한다.
- 아이콘 SVG 내용은 기존 추적 파일과 동일하게 유지한다.

## Current Status

- favicon 적용 경로가 정상이다.
- `npm.cmd run build`와 `npm.cmd run smoke:dist`가 통과했다.

## Next Steps

- 브라우저 캐시가 남아 있으면 강력 새로고침 후 탭 아이콘을 확인한다.
