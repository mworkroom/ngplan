# 2026-07-28 ngplan Project Development Log

## Work Summary

루트에 놓인 favicon 파일 때문에 기존 정적 자산 경로가 비어 있던 문제를 복구하고, 계획표 앱에서 favicon이 제공되도록 확인했다.

## Work Completed

- favicon SVG를 `public/assets/favicon.svg`에 복구했다.
- 기존 `index.html`의 `/assets/favicon.svg` 링크와 실제 파일 경로를 일치시켰다.
- 루트에 중복으로 놓인 favicon 파일은 제거했다.
- 프로덕션 빌드와 dist smoke check에서 favicon 자산 포함을 확인했다.

## Decisions

- Vite 정적 자산은 `public` 아래에 보관한다.
- 아이콘 SVG 내용은 기존 추적 파일과 동일하게 유지한다.

## Current Status

- favicon 적용 경로가 정상이다.
- `npm.cmd run build`와 `npm.cmd run smoke:dist`가 통과했다.

## Next Steps

- 브라우저 캐시가 남아 있으면 강력 새로고침 후 탭 아이콘을 확인한다.

## Work Summary - CI 테스트 문구 동기화

푸시마다 반복되던 GitHub Actions `CI / verify` 실패를 로컬에서 재현하고, 최근 UI 문구 변경으로 낡아 있던 테스트 기대값을 현재 화면과 맞췄다.

## Work Completed - CI 테스트 문구 동기화

- `App.test.tsx`의 오류 이동 버튼 기대값을 `첫 번째 문제 보기`에서 `문제 보기`로 변경했다.
- 회원 디렉터리 관련 테스트의 입력 라벨 기대값을 `피라미드 표시 이름`에서 `시트 표시 이름`으로 변경했다.
- 타입 검사, 전체 테스트, 커버리지, 빌드, dist smoke 검사를 다시 실행했다.

## Decisions - CI 테스트 문구 동기화

- 현재 UI 문구는 주사용자가 이해하기 쉽게 다듬은 상태이므로 UI를 되돌리지 않고 테스트 기대값을 동기화한다.
- GitHub Actions가 모든 push에서 같은 검사를 실행하므로, 문구 변경과 테스트 변경을 함께 유지한다.

## Current Status - CI 테스트 문구 동기화

- 변경된 테스트 기대값은 현재 UI 문구와 일치한다.
- 전체 56개 테스트 파일의 651개 테스트가 통과했다.
- 커버리지 기준, 타입 검사, 프로덕션 빌드, dist smoke 검사가 모두 통과했다.

## Next Steps - CI 테스트 문구 동기화

- J님이 원할 때 검증된 변경을 커밋·푸시한다.
