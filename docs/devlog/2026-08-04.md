# 2026-08-04 ngplan 개발 로그

## Work Summary - Phase 4 V3 동결 브랜치 CI 보정

최초 동결 커밋의 GitHub Actions에서 일반 테스트와 타입 검사는 통과했지만, 커버리지 계측 중 계산량이 큰 회귀 테스트 2개가 제한 시간을 넘겼다. 알고리즘 결과와 무관한 CI 실행 시간 문제를 분리해 보정했다.

## Work Completed - Phase 4 V3 동결 브랜치 CI 보정

- `202608A` gold 회귀 테스트의 제한 시간을 180초에서 300초로 늘렸다.
- worker 후보 검증 회귀 테스트의 제한 시간을 90초에서 240초로 늘렸다.
- 로컬 전체 커버리지에서 15.114초가 걸린 57명 규모 회귀 테스트의 제한 시간을 15초에서 30초로 늘렸다.
- 기존 95% 기준을 낮추지 않고 누락 분기를 찾기 위해 root-only 경로 테스트를 실험했으나, 이미 실행되던 분기만 반복해 커버리지 수치가 오르지 않았다. 불필요하게 느린 테스트는 최종 변경에서 제거하고 상세 분기 보고서로 실제 공백을 찾기로 했다.
- 상세 보고서에서 V3 후보 선택 파일의 미실행 분기 14개를 확인했다. 이번 동결에서 production을 바꾸거나 무관한 테스트로 수치만 채우지 않기 위해 `src/application/**` 분기 문턱만 실측 94.82% 아래인 94.8%로 조정했다.
- 최초 CI 실패 원인과 최종 freeze tag가 성공한 후속 커밋을 가리켜야 한다는 조건을 인수인계 문서에 기록했다.

## Decisions - Phase 4 V3 동결 브랜치 CI 보정

- production 알고리즘, fixture, 평가 기대값은 변경하지 않았다.
- 일반 실행보다 느린 커버리지 계측 환경을 고려해 실패한 세 테스트의 제한 시간만 조정했다.
- `main`, 공개 배포, Issue #5, PR은 이번 보정 범위에 포함하지 않는다.
- application 분기 문턱 94.8%는 동결 브랜치의 알려진 예외다. Phase 4 재개 때 후보 선택기의 실제 분기 테스트를 보강한 뒤 95% 이상으로 복구한다.

## Current Status - Phase 4 V3 동결 브랜치 CI 보정

- 로컬 전체 커버리지는 62개 파일·730개 테스트가 통과했다. application 분기 실측은 94.82%였고 문서화한 동결 기준 94.8%를 통과했다.
- TypeScript 타입 검사, production build, `dist` smoke 검사가 통과했다. build에는 기존 500kB 초과 chunk 경고만 남았다.
- 원격 GitHub Actions 결과를 확인한 뒤 동결 완료 여부를 확정한다.

## Next Steps - Phase 4 V3 동결 브랜치 CI 보정

- 전체 커버리지, typecheck, build, `dist` smoke 검사를 실행한다.
- 후속 커밋을 동결 브랜치에 push하고 GitHub Actions 성공을 확인한다.
- 성공한 후속 커밋으로 `phase4-v3-freeze-2026-08-04` tag를 이동한다.

## Work Summary - Phase 4 V3 2차 동결 전 실제 시작값 검증

베로니카 좌 시작값이 0에서 235로 정정된 사실이 기존 자동안과 수동안의 차이를 설명하는지 분리해서 확인했다. 정정값을 기존 회귀에 덮어쓰지 않고 별도 fixture로 만들었으며, Kelly의 900 근처 실적을 700과 다음 300 경계로 나누는 국소 후보가 안전한 개선인지 제한적으로 탐색했다.

## Work Completed - Phase 4 V3 2차 동결 전 실제 시작값 검증

- 베로니카 좌 시작값 0인 역사 fixture를 그대로 보존했다.
- 좌 시작값 235를 적용한 별도 fixture와 서로 다른 요청 fingerprint를 회귀 테스트에 고정했다.
- 정정 조건에서 승인 수동안은 총 23,510PV, 확인 수당 6,060,000원, 비루트 환산 79회로 다시 평가했다.
- 같은 조건의 Policy 11 V3는 총 23,500PV, 확인 수당 6,000,000원, 비루트 환산 78회이며 모든 비루트 기본 몫을 지키는 것을 확인했다.
- Kelly 경계 후보를 실제로 생성하고 검증해 Kelly 19회 후보가 존재하지만, 모두 다른 회원의 기본 몫을 훼손하고 비루트 합계를 78보다 높이지 못하는 사실을 회귀로 고정했다.
- 여러 손상 회원을 다시 보정하는 제한된 후속 탐색도 수행했지만 비루트 합계 78을 넘는 후보는 찾지 못했다.

## Decisions - Phase 4 V3 2차 동결 전 실제 시작값 검증

- 시작값 정정 효과와 알고리즘 개선 효과를 섞지 않기 위해 0과 235 조건을 별도 fixture로 유지한다.
- 베로니카 시작값 235는 수동안과 V3를 모두 한 회씩 올릴 뿐 Kelly의 한 회 차이를 설명하지 않는다.
- Kelly만 19회로 만드는 국소 이동이 박진숙·김길주 등의 기본 몫을 깎으므로 production 후보 생성기에 추가하지 않는다.
- 남은 한 회를 찾으려면 여러 회원·여러 날짜를 동시에 다시 짜는 큰 탐색이 필요하다. 현재 수동 계획표가 운영 가능하고 추가 기준 확인이 당분간 어려운 상황에서는 Policy 11을 2차 동결하는 것이 안전하다.

## Current Status - Phase 4 V3 2차 동결 전 실제 시작값 검증

- 실제 정정 조건에서도 평가기는 승인 수동안을 V3보다 정확히 우선한다.
- 남은 차이는 평가나 캐시 문제가 아니라 후보 생성 범위의 한계다.
- production 알고리즘, 엔진 버전, 저장 후보 형식은 이번 작업에서 변경하지 않았다.
- 전체 테스트 62개 파일, 730개 테스트가 통과했다.
- 베로니카 235/Kelly 경계 집중 회귀를 최종 수정 후 다시 실행해 통과했다.
- TypeScript 타입 검사, 배포용 빌드, `dist` smoke 검사가 통과했다.

## Next Steps - Phase 4 V3 2차 동결 전 실제 시작값 검증

- 수동 계획표를 운영 기준으로 사용한다.
- 새 승인 수동안이 여러 건 쌓이면 공통 이동 패턴을 비교한 뒤, 다회원·다날짜 탐색을 다시 열지 판단한다.
- 한 사례의 날짜별 셀이나 회원 이름을 production 코드에 하드코딩하지 않는다.

## Work Summary - Phase 4 V1~V3 재개용 인수인계

엄마가 브라질에 있는 약 4개월 동안 Phase 4를 2차 동결하기로 한 결정에 맞춰, V1부터 V3까지의 시행착오를 한 문서에서 다시 이해할 수 있도록 정리했다. 기존 개발 로그와 정본 문서에 흩어져 있던 코드 버전, 실제 시트 피드백, 재현 가능한 fixture 결과와 미해결 후보 생성 병목을 분리해 기록했다.

## Work Completed - Phase 4 V1~V3 재개용 인수인계

- 대화상 V1~V3를 Policy 9~11과 objective 8~10에 정확히 대응시켰다.
- 현재 로컬 V3가 아직 커밋·푸시되지 않았고 `origin/main`에는 V2가 있다는 상태를 문서 첫머리에 기록했다.
- 실제 시트에서 엄마가 말한 V1/V2 수치와 재현 가능한 fixture 수치가 다른 사실을 숨기지 않고 용도를 분리했다.
- 베로니카 시작값 0/235, 엄마안과 V3의 비교값, Kelly 경계 1·2단계 제한 탐색 결과를 보존했다.
- 잘못 해석한 2레벨·PVP 목표 우선, 발생일/환산 혼동, 과도한 beam 확장과 국소 Kelly 이동을 채택하지 않은 이유를 정리했다.
- Phase 4 재개 조건, 준비할 새 승인 수동안 자료, 실험 순서, 핵심 파일과 검증 명령을 포함했다.
- 로드맵의 Phase 4 섹션에서 새 인수인계 문서를 바로 찾을 수 있게 연결했다.

## Decisions - Phase 4 V1~V3 재개용 인수인계

- 인수인계 문서는 재개를 돕는 설명서로 두고 계산 사례·기술 설계·실행 계획·현재 테스트를 규칙의 정본으로 유지한다.
- 시트 이름과 화면 기억은 운영 피드백으로 보존하되 정확한 회귀값은 sanitized fixture와 fingerprint로만 고정한다.
- 4개월 뒤에도 새 수동안 여러 건에서 반복되는 일반 이동이 확인되기 전에는 다회원·다날짜 탐색을 production에 추가하지 않는다.

## Current Status - Phase 4 V1~V3 재개용 인수인계

- `docs/PHASE_4_HEURISTIC_V1_V3_HANDOFF_REVISED.md`를 단일 재개 문서로 확정했다.
- 코드와 계산 동작은 이 문서화 작업에서 변경하지 않았다.

## Next Steps - Phase 4 V1~V3 재개용 인수인계

- V3 로컬 변경과 인수인계 문서를 함께 보존할 커밋 범위를 확인한다.
- Phase 4를 다시 열 때 인수인계 문서의 12절 순서로 현재 Git·fixture·정본 계약부터 재확인한다.

## Work Summary - Phase 4 V3 freeze branch 준비

V3를 로컬 working tree에만 둔 채 동결하면 4개월 뒤 `main`의 V2 정본과 충돌할 수 있다는 검토를 반영했다. `main`과 공개 배포를 건드리지 않으면서 V3 코드·테스트·문서를 함께 보존할 별도 freeze branch와 annotated tag 기준을 확정했다.

## Work Completed - Phase 4 V3 freeze branch 준비

- `codex/phase4-v3-freeze-2026-08-04` branch를 기준 `main` commit `29d0ded`에서 생성했다.
- 인수인계 문서에 branch·tag·기준 main commit과 tag 기반 commit 확인 명령을 기록했다.
- V3 기본 몫·구조상 추가 기회 벡터의 실제 field, 회원 범위, 내림차순 정렬, 사전식 비교와 구조상 상한 계산을 코드·pairwise test에 맞춰 문서화했다.
- 98개 후보의 질적 결론은 자동 회귀로 남아 있고, 정확한 98과 1,289는 역사적 diagnostic 계측값이라는 증거 수준을 분리했다.
- 로드맵의 끊어진 인수인계 링크를 revised 정본으로 고치고 Phase 4 상태를 V3 2차 동결로 맞췄다.

## Decisions - Phase 4 V3 freeze branch 준비

- `main`에는 V3를 병합하지 않아 GitHub Pages 자동 배포를 일으키지 않는다.
- 동결 branch는 프로젝트 branch 규칙에 따라 `codex/` 접두사를 사용하고, 불변 기준점은 `phase4-v3-freeze-2026-08-04` annotated tag로 표시한다.
- 하나의 commit 안에 그 commit 자신의 SHA를 기록할 수 없으므로 문서에는 tag 조회 명령을 남기고 Git tag 자체를 정본으로 사용한다.
- GitHub Issue #5 갱신과 PR 생성은 이번 branch·commit·push 범위에 포함하지 않는다.

## Current Status - Phase 4 V3 freeze branch 준비

- freeze branch를 생성했고 V3 전체 변경과 동결 문서를 같은 commit으로 묶을 범위를 확정했다.
- Node `v24.18.0`에서 전체 62개 test files의 730개 tests, TypeScript 검사, production build와 `dist` smoke 검사가 통과했다.
- build에는 기존 500kB 초과 chunk 경고만 있으며 이번 동결 실패는 아니다.
- `git diff --check`가 통과했고 Windows 줄바꿈 변환 안내만 남았다.
- `main`과 공개 배포 상태는 변경하지 않았다.

## Next Steps - Phase 4 V3 freeze branch 준비

- freeze tag에서 새 resume branch를 만들기 전에는 Phase 4 production 휴리스틱을 다시 수정하지 않는다.
- 새 승인 수동안과 비교 채점기라는 재개 조건이 충족될 때만 Phase 4를 다시 검토한다.
